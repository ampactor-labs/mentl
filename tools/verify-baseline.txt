# tools/verify-baseline.txt — the Mentl-thesis gate's recorded baseline.
#
# tools/verify.sh (and the .githooks/pre-commit hook that runs it) BLOCKS a
# commit if any micro in the battery below regresses, the wheel does not
# compile, or the census ratchet rises. That, plus drift-audit (the Carried-
# Truth Law, run by the same hook), IS the gate: the wheel COMPILES AND RUNS
# correct code, the source coheres, and the medium's verdict on its own source
# never gets worse.
#
# ── WHERE THE CENSUS BOUNDS WENT ─────────────────────────────────────────
# 2026-09-24: five more — comment_refs_max, field_offset_unprovable_max,
# eq_type_unprovable_max, authored_own_max, authored_ref_max. They were read
# here at COMMIT, after the repin had blessed the wheel, so a repin could pass
# and its commit refuse (one landing paid a fourth march for exactly that).
# `mentl verify` runs the compile's remainder now and reads the first three
# off the compile's own tally, and the ownership pair is a census over the
# parameter product's `authored` slot rather than a regex over the page. Their
# histories are in git; their justifications are beside their numbers in
# src/board.mn.
#
# Twelve keys left this file: eta_max, effectful_lambda_max,
# record_pattern_open_max, wildcard_zero_max, failure_mask_max,
# print_in_report_max, wildcard_fabricates_max, underscore_retain_max,
# flag_as_int_max, parallel_arrays_max, vtable_record_max, env_frame_max.
# They live in src/board.mn now, each bound beside the justification that
# argues it, where `mentl why` can walk the pair and `mentl verify` reads the
# count off the one judged graph instead of grepping a number out of prose.
#
# They had been dead here for a landing: verify.sh delegates the board to
# `mentl verify`, so nothing read these keys, and 646 lines — 38% of this
# file — stood as a second home for bounds that had moved. That is the
# Carried-Truth Law at the gate layer, inside the file whose whole job is
# holding one number per invariant. The keys that remain are the ones with a
# live reader: what the medium cannot yet measure about itself, or what is
# not a census at all (peak RSS, the frontier's expected-red roster, the
# authored-marker counts, the desugar vocabulary contract).
#
# ── THE CENSUS, RE-FOUNDED (2026-07-16) ──────────────────────────────────
# The paragraph that stood here said: "an informational SHADOW, enforced
# NOWHERE (Morgan, 2026-06-22) ... the disposable seed's weaker inference lags,
# so a rising census is the seed catching up to the wheel, expected progress"
# and "census_max no longer exists; it is read by nothing."
#
# THE SEED WAS DELETED eighteen days after that was written (7401c4b,
# 2026-07-10, "Fly, my pretty <3"). boot/mentl.wasm IS the wheel — the pinned
# fixpoint of the very source it compiles. So m2.err is not a weaker seed's
# shadow of a stronger wheel. It is the WHEEL's diagnostics about the WHEEL's
# OWN SOURCE: 2,266 claims the medium makes about itself and does not believe.
# The justification expired at first light; the number kept its old alibi for
# six days, and under it sat a real dead-code bug — src/format.mn matched
# `NPipeExpr`, a constructor declared nowhere (the real one is `PipeExpr`,
# types.mn:917), so format_chain's five verb arms never matched and every chain
# fell through the `_` catch-all. The compiler named it six times, with spans.
#
# So the census RATCHETS: errors may never rise. It is the transport toward the
# refusal law (PLAN §11 column 2) — the endpoint is `mentl check` on the wheel
# exiting 0, at which point emit refuses on any error and this counter is
# DELETED (PLAN §6's scaffold tier: it exists to dissolve). Warnings are
# reported, not ratcheted — they are the format-lift backlog and the formatter
# erases them by construction.
#
# census_errors   = wheel-compile stderr lines matching 'E_… error: '
# census_warnings = … matching '(E_|W_|P_)… Warning: '   (reported only)
#
# 2026-06-15: bumped 171 -> 190 (deliberate, explained). The field-pair-name
# fix (walk_compound.wat $field_byte_offset) corrected record field-offset
# resolution: multi-field `.field` previously collapsed to offset 0 (the seed
# read a field_pair RECORD via list_index). With correct offsets, lowering
# reaches 19 more sites where a record-field type is still NFree at lower time,
# each emitting a productive `E_UnresolvedType: lower-time NFree` (56 -> 75).
# E_TypeMismatch (70) / E_MissingVariable (37) / E_FeedbackNoContext (8) are
# unchanged — no new real errors. These 19 NFree are the next ratchet-down
# target (seed record-field type-resolution completeness). The fix is a hard
# pass-2 prerequisite: it clears the grow_to_with_gaps OOB (placement.slot was
# reading ename, a string pointer).
#
# 2026-06-21: bumped 190 -> 201 (deliberate, explained). Census is a SHADOW
# (Morgan 2026-06-21): the real first-light oracle is the FIXED POINT (m3==m4)
# + micros + the pass-2 trap-march, NOT the diagnostic count. The type-
# parameterized-effects substrate (EAType; register_effect_ops full var set;
# subst_ty/instantiate chase-to-root) RESOLVED 41 E_UnresolvedType (75->34:
# state-field types that were NFree at lower time now resolve) at the cost of
# 56 more E_TypeMismatch (70->126: the medium now CONNECTS types it previously
# left free — proof-beats-review honesty, in-progress, expected to ratchet
# back DOWN as the offset-flow completes). The handler_state_inits_registry
# dissolution (this commit) carried the census DOWN 204->201 and cleared the
# filter-in-arm trap. Verified: micros 7/7, seed compiles the wheel, drift
# clean, no dangling refs to removed symbols, pass-2 trap marched filter ->
# lower_one_arm_decl/lower_expr (deeper = progress). Next ratchet-down: those
# 56 E_TypeMismatch + the lower_expr trap.
#
# 2026-06-21: bumped 201 -> 202 (deliberate, explained). The headline change is
# NOT census — it is that the emit CORRUPTION (a silent resolve_field_offset
# `_ => 0` fallback: an unresolved-type field access returned offset 0, read the
# node's word-0 = a graph pointer, and wat_emit dumped 150GB) is DELETED. The
# ultimate form has no silent surrender: resolve_field_offset returns -1 (both
# layers), and the ONE load-offset primitive ($el_emit_i32_load_offset / the
# wheel's emit_load_chain) guards off<0 -> (unreachable). m2 now ASSEMBLES and
# RUNS, and the corruption is a CLEAN, LOCATABLE trap at lower_scope ls_push_scope
# (frame.local_order on a still-NFree List element type — the next root). That is
# the medium naming its own unprovable offset (the first sliver of mentl audit),
# the real oracle (fixpoint + micros + trap-march), NOT the count. The +1 itself
# is the inline-handler arity fix (infer_handler_arms now mints + passes payload_h;
# the prior 2-arg call to the 3-param infer_one_handler_arm left it garbage):
# lib/dsp/processors.mn's inline `handle { } with { }` arms are now ACTUALLY
# inferred, surfacing 1 real relationship while E_TypeMismatch dropped (126->123)
# — net more-correct inference. Verified: micros 7/7, seed compiles the wheel,
# drift clean. Next ratchet-down: the lower_scope handler-state element type.
#
# 2026-06-21: ratcheted 202 -> 181 (DOWN = progress). ResumeExpr inference now
# unifies each `with field = expr` state update with the state field's type
# (both layers: src/infer.mn + the seed's $infer_walk_expr_resume, which had the
# updates as "second field unused"). The updates were DISCARDED — a Carried-Truth
# violation — so a state field stayed at its init type forever (an empty-list init
# = List(?free)). Carrying the update's type in resolved 29 E_UnresolvedType
# (34->5); E_TypeMismatch +8 (the medium now connects those formerly-free state
# types — proof-beats-review, expected to ratchet down as offset-flow completes).
# Verified: micros 7/7, seed compiles the wheel, drift clean.
#
# 2026-06-22: census RETIRED as a gate (Morgan). The ultimate .mn leads; the
# disposable seed's weaker inference lags and catches up — a rising census is
# the seed catching up to the wheel, NEVER a reason to refuse the wheel. The
# old census_max ratchet contradicted this file's own 2026-06-21 verdict
# ("census is a SHADOW; the real oracle is the wheel working + coherence, NOT
# the diagnostic count"). The gate is now: the seed builds + the micros
# compile-and-run (the wheel WORKS) + the drift-audit (the Carried-Truth Law).
# verify.sh reports census as an informational shadow, enforced nowhere. The
# micro battery below IS the gate. (census_max removed — read by nothing.)
#
# 2026-07-16: the entry above was CORRECT while the seed existed and expired
# with it eighteen days later (7401c4b). Every clause of it names the seed;
# none of them survives boot-IS-the-wheel. The census is a ratchet again — not
# by overruling the 2026-06-22 call, but because its subject changed: it no
# longer measures a weaker compiler's shadow of a stronger source, it measures
# the medium disbelieving itself. See the re-founding at the head of this file.

# ── THE RATCHET ─────────────────────────────────────────────────────────
# The medium's error-verdict on its own source. It may never rise. Lower it
# whenever it falls; the bar (PLAN §11) is 0, at which point the refusal law
# turns on and this line is deleted.
#
# 2026-07-16: 2282 -> 2256. Re-founded from the expired seed alibi (above). The
# first fall came free once the number was looked at: format.mn's `NPipeExpr`
# ghost (24 errors) was a partial rename left half-done — the same file already
# used the real `PipeExpr` at lines 341 and 485, and a `_ => render_non_chain`
# catch-all swallowed the five dead arms.
#
# 2026-07-16: 2256 -> 2258 (deliberate, +2 E_TypeMismatch, the medium learning
# to see). infer_binop's BKConcat arm contributed NO row, so `++` was attributed
# Pure while the str_concat/list_concat it lowers to declare `with Memory +
# Alloc`. Measured before/after on the same file, `fn viaop(ref n) with !Alloc =
# "a" ++ n`:
#     old: T_OverDeclared "body only uses Pure — tighten the signature"; Pure
#     new: E_EffectMismatch !Alloc vs Alloc + Memory;      Alloc + Memory
# The old path did not merely miss the false !Alloc — it ADVISED tightening into
# it. E_EffectMismatch on the wheel is UNCHANGED at 92 (the wheel already
# declares Memory + Alloc where it concatenates), so the +2 is two type conflicts
# the row now connects, not a regression. This is the 2026-06-21 shape recurring:
# "the medium now CONNECTS types it previously left free — proof-beats-review,
# expected to ratchet back down."
#
# 2026-07-16: 2258 -> 2266 (deliberate, +3 E_EffectMismatch / +5 E_PurityViolated).
# The re-pin compounds the fix through the fixpoint: boot is now the wheel that
# knows `++` allocates, so the medium re-judges its OWN source with its own
# sharper eye and catches eight fns whose declared row is narrower than their
# body — `with EnvRead` over a body that really uses Alloc + EnvRead + Memory.
# The eight, by name: any_imports · collect_hash_tys_expr · collect_hash_tys_list
# · collect_show_tys_expr · ctor_payload_tys · cursor_argmax_compute ·
# emit_distinct_compare_helpers · emit_distinct_eq_leaves · emit_distinct_hash_leaves
# · emit_distinct_show_leaves · emit_eq_leaf_sum · emit_fold_hash_helpers.
# These are TRUE findings — the declarations are false, and they were false
# before this fix; only now can the medium say so. They are NOT fixed here: each
# widening ripples into the callers' own declared rows, and starting that cascade
# without measuring it is how a "small fix" eats a wheel. That measurement is the
# next ratchet-down, and it is the shortest path from here toward the bar.
#
# 2026-07-17: 2266 -> 1904 (DOWN 362, Stage 1b). Deleted check_ref_escape — a
# purely syntactic escape check with no hazard model that fired on use-count-plus-
# bare-return, flagging `min(a,b)` and every fold accumulator. 356 of those were
# the exact "escapes its scope (returned)" shape; deleting the checker cleared
# them and 6 downstream, and the region tracker (check_escape, E_RegionEscape = 0)
# already proves the real lifetime hazard. Net -55 source lines; the emit shrank
# 1,813 lines (the checker's own body, gone). This is the ratchet doing what §5.O
# could not: a class cleared by DELETION, not by a scan made faster — but note the
# re-founding (head of file): the count is the transport, the ARMED refusal arm is
# the bar. This class does not arm (there is nothing left to detect); it simply
# stops lying. E_MissingModule remains the one armed class.
#
# 2026-07-17: 1904 -> 1337 (DOWN 567, Stage 1a core ADTs). Typed the erased
# element of the compiler's own core ADTs: Ty (TTuple/TFun/TName/TRecord/
# TRecordOpen), Node (CallExpr/BlockExpr/MatchExpr/ResumeExpr/Make*/...), Pat,
# LowExpr, LowPat, LowFn, Scheme, SchemeKind, EffRow, and the shared handler-arm
# / state-field record shapes across types/parser/lower/infer. A bare `List` is
# TName("List",[]) — a nominal name that never unifies with the native TList the
# consumers build via `[..]` literals and list ops (there is no bridge, by
# design: SYNTAX §4① has ONE sequence kind, `[a]`, and the nominal `List` is the
# drift). The fix is [Element] at the declaration, never a TName<->TList unify
# bridge (which would legitimize the illegal shape while leaving the element
# erased). Same sweep cleared `N vs Node` (14 -> 0): `body: N` wrote the
# constructor name where the type Node was meant. Inference-only — m2 == m3
# byte-identical, zero new error classes. 581a92f swept only the trap-causing
# subset and closed "the census after the next march is the arbiter"; this is
# that census, the siblings finally swept.
#
# 2026-07-17: 1337 -> 1233 (DOWN 104, Stage 1a leaf files). The same [Element]
# fix carried into the leaf ADTs and effect-op signatures the core sweep did not
# reach: query (QueryResult), voice (Answer/CompileOutcome/CheckOutcome/
# AuditReport — the comments named Diagnostic/AuditFinding, types that do not
# exist; the real element is DiagKind / the debt triple, read from the producer),
# lsp/oracle/json responses, autodiff ([Float]/[TapeEntry]), mentl synth
# (Proposals/Context/enumerate_inhabitants), infer (inf_enter/exit_fn effs,
# set_summaries), the wasm drain_* emit collectors (each named by its visit_*
# companion), cursor/eight_loop projections, the StringTable/BodyContext ops, and
# cache_map (parameterized CacheMap(v) — a generic container, correctly). The
# true bare-List shape (List(X) vs List) is now 6, from 645+ at the start: the
# residue is buffer.mn (a dead module — no import, no external caller; a deletion
# candidate, not typed here) plus a few genuine mismatches that were never this
# class. What remains in the census is the OTHER roots the plan already named —
# ~144 ETypeArityMismatch, and genuine errors — not List erasure. Inference-only;
# m2 == m3 byte-identical; zero new classes.
#
# 1233 -> 874 (2026-07-17, Hβ.infer.alias-preserving-unify LANDED). The 362
# Span-vs-ValidSpan / Int-vs-ValidOffset errors were ONE root: pre_register_decls
# ran a single source-order pass, so a fn signature quantified before its refined
# alias was declared (parser.mn's `span: ValidSpan` precedes types.mn) read a
# MISSING env edge and baked a bare nominal TName the main walk could never refine
# — unify(bare-TName, base) floored at the leaf. Fix: register the alias edges in
# a phase BEFORE any fn signature (pre_register_alias), so quantify_ctor_ty reads
# the LIVE edge. Carried-Truth: the graph drew the alias; the resolver re-derived
# a name. The banked "two-pass is a no-op" was measuring boot's census (old
# inference), not the fixed compiler's — the fixed compiler's census is 0 for the
# class. Emit byte-identical (error-hole and resolved-TAlias both emit word width,
# same codegen), so a clean m2 == m3 fixed point; boot re-pinned because the
# compiler binary changed. Guarded RED-first by
# tests/frontier/mn-refined-alias-forward-ref.mn.
#
# 874 -> 727 (2026-07-17, Hβ.infer.bare-parameterized-type-arity LANDED). The
# arity `0 vs 1` class (145 errors) was ONE shape, one type over from Stage 1a's
# bare List: a bare parameterized type — mostly `Option`, plus a couple of tuple
# arities — written WITHOUT its argument in a declaration, meeting the real
# `Option(X)` / `List(X)` its consumers build. The root was `env_lookup(String)
# -> Option` (types.mn) — the effect op erased the `Option((Scheme, Reason,
# SchemeKind))` its own handler proves via env_resolve, so every env_lookup match
# site (~80) mismatched. The sibling declarations: the Annotation ctors
# (Option(Span)), the teach/CursorView gradient field (Option(AnnotationSuggestion)),
# PList/LPList rest (Option(String)), gradient_pop/step (Option(Cursor)),
# inf_arm_tys (Option(Int)), ls_escaping_of (Option(EffRow)), the Situation /
# TopicFacts record fields, the Lsp response ctors, Explanation's fix (Option(Patch)),
# QRHandlerProvider (Option(String)), tree_list ([TreeEntry]), NonEmptyList ([a]),
# and QRIntent's tuple ([(EffName, Bool, EConn)]). Carried-Truth: each consumer /
# handler already proves the type; the declaration erased it. Pure declaration
# fix — emit byte-identical, boot UNCHANGED (an error hole and a resolved
# Option(X) lower the same), so no re-pin; the census ratchet IS this class's
# gate (revert and it rises past 727). Residue: Buffer needs `Buffer(a)`
# parameterization (7 sites, genuinely generic) — named, not forced here.
#
# 727 -> 615 (2026-07-17, the affine ownership model, two Carried-Truth fixes).
# 115 of the 152 "consumed twice" were false positives from two masked bugs:
# (1) if/match arms were NOT bracketed as branches, so an `own` value read in
# both arms of `if i<0 {slice(buf)} else {list_set(buf)}` counted as two
# consumes — but the arms are ALTERNATIVES (one runs), so it is one consume on
# the taken path. Fix: a BranchMode ADT (BParallel | BAlternative) rides the
# branch frame; branch_exit reports collisions only for BParallel (`><`/`<|`,
# both run), unions without collision for BAlternative (if/match). (2) the
# affine_ledger is installed ONCE at the pipeline, not per body, so a name
# consumed in one function stayed in `used` and collided in the next function
# naming the same `own` param (`buf`/`acc` are everywhere) — the dominant
# shape. Fix: consume_enter_fn/consume_exit_fn bracket each infer_fn body, a
# fresh consume scope restored on exit (re-entrant for nested fn decls). Emit
# grew (the ADT + ops), a clean m2 == m3 TRANSITION, boot re-pinned. Guarded
# RED-first by tests/frontier/mn-own-alternative-branches.mn. E_IfMissingElse
# rose 29 -> 32 — productive-under-error exposing real if-without-else the
# masking ownership errors hid, not a regression (total still fell). Residue
# (37 left): the move-vs-borrow-by-callee case — an `own` value read in an `if`
# CONDITION (`if set_contains(buf,...)`) or a field (`buf.data`) is a BORROW,
# not a move; only a whole-value move to an `own` param should consume.
#
# 615 -> 583 (2026-07-17, the borrow scope — move-vs-borrow, part 1). An `if`
# condition and a `match` scrutinee are READ to decide control, never moved, so
# a use inside them is a borrow. A borrow_depth counter on affine_ledger +
# borrow_enter/borrow_exit bracket the condition and scrutinee; consume no-ops
# while borrow_depth > 0. Cleared 32 of the 37 (`if set_contains(buf,...)` &c.),
# soundness preserved — a real double-move `(take(buf), take(buf))` to two `own`
# params is still caught. Emit grew → clean m2 == m3 transition. Residue (5):
# the field-read (`buf.data` + `buf.count`, buffer.mn) and the sequential
# call-arg-borrow (`len(nodes)` then `list_copy_into(nodes)`, graph.mn) — the
# remaining move-vs-borrow needs a field-receiver borrow + the callee-param
# ownership at each call arg (a borrowing param does not consume its argument).
#
# 583 -> 582 (2026-07-17, move-vs-borrow part 2 — the field-receiver borrow).
# Reading `.field` borrows the receiver; it never moves the record. infer_field
# _access_record brackets the receiver in a borrow scope when it is a value
# chain (a VarRef or nested field access) — `f(x).field` keeps its normal
# inference so `x`'s move into `f` still consumes. `buf.data`/`buf.count`
# (buffer.mn) are now two borrows, not two consumes. Soundness intact, clean
# m2 == m3 transition. Residue (4, all `graph.mn`/`voice.mn`): the call-arg-
# borrow — `len(nodes)` then `list_copy_into(nodes)`, an `own` value passed to
# a BORROWING parameter. The last move-vs-borrow piece: read the callee's param
# ownership at each argument so a ref/unmarked param does not consume its arg.
#
# 582 -> 578 (2026-07-17, move-vs-borrow part 3 — callee-param ownership).
# Call inference previously inferred every argument before reading the callee's
# parameter product, so `observe(ref value)` consumed an owned caller value just
# like `take(own value)`. The call now reads the canonical TParam product after
# labeled/default argument resolution: a direct variable passed to an authored
# or inferred Ref is borrowed; an Own argument and every nested computation keep
# normal move inference. Runtime `len` and `list_index` declare their actual
# read-only Ref access, and update_file_text_loop carries list_set's returned
# owner instead of discarding it. E_OwnershipViolation is 0 on the fixed
# compiler's own m3 judgment; the real `take(value) + take(value)` double move
# remains rejected. Guarded red-first by mn-own-call-arg-borrow,
# mn-own-forward-ref-seq, and mn-own-call-arg-move. Clean m2 == m3 fixed point.
# 356 -> 352 (2026-07-18, the SMT handler tells the truth). verify_smt declared
# only `witnesses` while its arms update and read `debt` — the SMT unknown-debt
# ledger never existed as state, so every arm reference was an unbound name (2
# E_MissingVariable + their 2 paired E_OccursCheck, the missing name minting a
# self-referencing free var). Adding `debt = []` is the whole fell. Same commit:
# the solver's polarity (validity = UNSAT of the negation — SAT-of-the-predicate
# admitted any satisfiable open predicate as discharged; predicate_decide already
# short-circuits ground ones, so the solver only ever sees open predicates);
# node_to_predicate's missing BNe arm (`self != 0` fell to the opaque PBoolNode
# and accrued debt instead of deciding — one arm: PNot of the BEq compare); and
# show_pred_operand's `<expr>` fabrication on compound operands (a refinement's
# own `-1.0` rendered as `<expr>` in the very diagnostic that exists to teach
# the bound — UnaryOp/BinOp now render recursively).
# 352 -> 348 (2026-07-18, one miss one diagnostic + the handler-namespace
# refusals). The env-miss path bound TVar(self) — tripping graph_bind's own
# occurs check (a spurious 0:0 E_OccursCheck per missing name) and reading
# downstream as an unconstrained var (the ownership move-default cascade the
# adversarial panel proved with own1.mn). It now binds NErrorHole via the new
# graph_bind_hole op: one miss, one diagnostic; unify no-ops, generalize
# preserves, an unresolved VarRef callee borrows its direct args. The voice
# handler's two state-shadows (history, caret — each naming an op of its own
# handled effect; the collision measured compiling clean with a WRONG value)
# renamed to turns/caret_now, and the class REFUSES at decl from birth
# (E_HandlerStateShadowsOp, armed; the medium's own m3 leg caught `caret`
# the same hour the check landed). E_DuplicateFnName likewise born armed
# (two `fn dup` emitted a WAT redefinition only wat2wasm caught). The dead
# duplicate FeedbackSpec (types.mn) deleted — its Fb* ctors had zero code
# consumers while silently sharing tags with prelude's Delay/Accumulate/
# FilterSpec. E_MissingVariable and E_OccursCheck are at ZERO on the wheel
# but do NOT arm: the panel refuted blob-zero as the licence (user paths
# still fire them on unresolved stdlib vocabulary until the manifest work
# lands — no class arms until its user-path census is also clean).
# 348 -> 343 (2026-07-18, the bounds trap + short-circuit boolean verbs). SYNTAX
# §Indexing's "traps on out-of-range" was PROSE: list_index tag-0 raw-loaded, so
# every OOB index silently read adjacent memory (the adversarial panel's find
# while refuting the elision landmine). The checked entry landed with its
# structural prerequisite: `&&`/`||` now SHORT-CIRCUIT (lower's BKBool arm — the
# boolean verbs are control, not arithmetic). The eager i32.and had run every
# guarded read: set_insert, cache_filter_loop, and register_one_variant all read
# one-past under their own guards (nullary ctors probed list_index(_, 0) of an
# empty list on every `type X = A | B`) — three latent OOBs the trap surfaced in
# its first hour. The dead narrowing-elision write (never fired — PAnd
# left-descent to PTrue) is deleted; the discharge-gated write is the named peer
# Hβ.infer.narrowing-write-requires-discharge. TRANSITION m3 == m4 (the emit
# change crossing one generation); micro oob-traps=134 is the law's executable
# form. The -5: guard rewrites cleared 4 E_TypeMismatch + 1 E_IfMissingElse.
# 343 -> 301 (2026-07-18, two checker roots — the medium's own diagnostics were
# the bugs). E_IfMissingElse EXTINCT (31 -> 0): the unit test matched
# TName("Unit") — a nominal spelling the inferencer never produces; real unit is
# TUnit, so every genuinely-unit then (`if c { wat_emit(...) }`, 25 sites) was
# judged non-unit, and 6 more were unresolved-at-check-time op results the unify
# below resolves (the probe that settled it printed then_ty=() for all 25).
# E_FeedbackNoContext EXTINCT (11 -> 0): the check read iter_context() — the
# COMPILER'S runtime handler stack — while inferring the COMPILED program's
# structure; a category error that fired on every real `<~` in the wheel,
# SYNTAX's own canonical lowpass included. The honest compile-time question
# (does the site's ambient row carry an Iterate-CLASS effect?) needs class
# membership as a declared fact: Hβ.effects.iterate-class-declaration. The
# DiagKind stays for the check's return.
# 301 -> 213 (2026-07-18, the effect-truth sweep, round 1). The census-spec
# fleet (29 readers, one per file) produced exact edit specs for 192 sites;
# 148 applied this round (the rest held for the concurrent design panel's
# read-set): declared rows widened to the body's truth (never a row-var into a
# declaration), aspirational `with Pure` dropped for the honest Memory/Alloc
# the bodies perform, missing match arms written explicitly with correct
# payload arities, the LStateSlotStore patterns bound their fourth field.
# EffectMismatch 95 -> 70 (the remainder = the held files + the widen
# cascade's second round), PurityViolated 56 -> 24, PatternInexhaustive
# 25 -> 7, ConstructorArity 4 -> 1.
# 213 -> 185 (2026-07-18, the cursor-address transport + the widen cascade's
# second round + the render's own row bills paid). `mentl voice.mn:9` is REAL:
# VAt parses at the argv boundary (shape-only byte walk; the runner resolves
# like every verb), driver_entry_with_ranges carries the module-range map out
# of the concatenation fold it always computed, the three-case line rule
# resolves over the span index every node writes at birth, cursor_at_handle
# projects the same eight-arm CursorView mentl edit reads (cursor_at's
# exact-match reason-span scanner DELETED for find_tightest over mint spans —
# the fleet-flagged fabricating resolver retired), and the terse facet-silent
# render reads the source slice, the env scheme for decls, literal bodies for
# free cells, and the Why walk through the range map. propose_at is now the
# ABSENCE facet structurally (a bound node's proposals are empty by
# construction) — which also fences the float-evidence call-convention gap
# (an f64-arg candidate ctor through an all-i32 $ft: the named peer
# Hβ.emit.float-evidence-ft, first fired by the first float-position
# projection ever taken). ~55 more declared rows widened to their bodies'
# truth by the mechanical round (the re-pinned sharper compiler re-judging).
# 185 -> 116 (2026-07-18, the effect-truth family EXTINCT). The widen loop ran
# to its fixpoint (nine iterations, each rebuilding and re-judging): declared
# rows to the bodies' truth, negation-carrying declarations matched on their
# positive parts with the ! entries preserved. E_EffectMismatch 95 -> 0 and
# E_PurityViolated 66 -> 0 across the arc — the two classes born from the
# ++-carries-its-callee's-row honesty fix (2026-07-16) are gone; the wheel's
# declarations now tell the truth the bodies prove. Remaining: the
# TypeMismatch tail (105, root-grouped), UnresolvedType 4, Pattern-
# Inexhaustive 4, ResumeOutsideArm 2, ConstructorArity 1.
# 116 -> 101 (2026-07-18, five more classes extinct). ResumeOutsideArm: the
# synth candidate fan moved INTO its arm as a nested fn (backtrack's own
# try_each shape — resume is context-bound to arms, and the census was right).
# ConstructorArity: QRFlowLabel's second field bound. PatternInexhaustive: the
# exhaustiveness counter was PAlt-BLIND — collect_arm_tags matched only a
# top-level PCon and arms_have_wildcard only a top-level PWild/PVar, so two
# H6-exhaustive alternation matches in voice read "misses 11 of 13" while
# covering every variant; both walks now flatten alternation branches. The
# refutable let-destructure in register_one_variant became the match it
# always was (the guard's proof read once); unlock_capability gained its
# missing AWrapHandler arm (containment -> CSandbox); the dead
# lib/runtime/buffer.mn DELETED (zero consumers, a lying row, a bare-List
# residue — three finds, one deletion). Remaining: E_TypeMismatch 97
# (root-grouped: Ctor({...}) 29, list-word boundary 21, misc 16, feedback
# recurrence 15, seq-op-hole 14 — minus the 8 these restructures already
# resolved), E_UnresolvedType 4 (handle-level, probes next).
# 101 -> 73 (2026-07-18, the record-ctor arrow view). `X({...})` — a
# single-variant nominal record's constructor in the ONE application syntax —
# met the env's RESULT binding (TName under RecordSchemeKind) at the call's
# plain chase and mismatched at 29 sites, the tail's biggest root. The ARROW
# is a VIEW minted at the read (record_ctor_arrow_view — the identical
# instantiation the pattern path's RecordSchemeKind arm already performs; one
# view, two readers; the env's scheme stays the RESULT, one home) and the
# ordinary saturated-call machinery does the rest. TRANSITION m3 == m4 (the
# view changes inference; the new wheel reproduces itself). Remaining:
# E_TypeMismatch 69 (list-word boundary 21, feedback recurrence 15,
# seq-op-hole 14, misc — the banked root grouping), E_UnresolvedType 4.
# 3 -> 0 (2026-07-26, the value-boundary landing): the raised lane closes.
# The trio's banked diagnosis ("rounds lose the subtraction") was refuted
# by measurement — the root was a five-link conjunction ending at the
# T-handle row-var class: a sort-blind row_var_is_free made both of
# infer_context's scheme row tails invisible to quantification while
# subst_row's catch-all SHARED them, so every caller chained one live row
# and the first binder's thunk row became everyone's mismatch. With the
# three-walk tolerance (+ the mask-carry at subst, the creation-edge
# delete, the loose-scheme share-guard, the cone-gated prepass) the whole
# self-compile raises ZERO row mismatches and the rounds CONVERGE.
census_errors_max: 0

# 2026-07-31: the MOVERS ratchet — the condemned second pass's vital sign
# (PLAN, THE RESOLVED DESIGN D5/D8): the count of schemes the final judges
# differently than the trial published. Overrides, not verification — so this
# number is DIRECTION, ratcheted monotone DOWN: rung 3 ends at 0 with the
# final pass DELETED, at which point this key dies with the channel it
# watches. A rise is the tower regrowing (Anchor 2's condemned clause) and
# blocks the commit. Seeded 1446 at the guard half-step's working tree;
# ratcheted to the restored pin's measured 930 when the half-step
# reverted (2026-07-31, the HALF-STEP REVERTS ledger entry). Raised to
# A1's measured 956 (2026-08-01, fixed-input-probe-justified: 48 movers
# byte-identical under both judges). Re-baselined to 1197 at pin
# 1efe083a (THE PROOF IS THE INTERFACE): the publish law changes
# published rows wholesale, so the trial/final comparison surface
# moved with the semantics — the fixed-input probe answers 49 vs 48
# (same families, one shifted with the publish), so the wheel-scale
# rise is the re-baseline, not regression. RATCHETED DOWN 1197 → 415
# at pin 13631390 (the one-way-edge arc: verbatim-edge charges, the
# union, signature quantification, the completion prune — read from
# the m3 leg's own movers total, the metric's largest single fall).
# The next legal move stays DOWN (A4 + rung 3 drive toward 0, where
# this key dies). Fell 415 → 414 at the index-sugar row charge (the
# judgment charging what the lowering performs moved one mover), then
# 414 → 406 at the constructor-row charge (pin 4a822b299c — eight
# trial/final divergences were constructor-attribution asymmetries;
# the surviving 406 are the fill/float trial-only channel).
# Re-baselined 406 → 424 at the signature keep (the crown's higher-order
# fix): kept sig-reachable row edges change what the HOF family PUBLISHES
# (the flip census names map_list / filter_list / flat_fill_* / the
# float-digit fns — the exact fns whose param row vars now ride their
# rows), so the trial/final comparison surface moved with the semantics —
# the 1197 publish-law re-baseline's own precedent, the same channel one
# fix over. Not machinery regrowth: the final's verdict is what
# publishes, and the +18 are pre-existing freeze-root divergences (the
# schemes-are-values root, PLAN §7) newly VISIBLE on rows the prune used
# to close. Re-based 424 → 439 at the residual-reads-the-install landing
# (the same channel, the third application: the handler-installing
# vocabulary — count joins the same flip family — now publishes rows
# carrying the config-fn joins, so the trial/final surface moved with
# the semantics again; zero new trial/final machinery in the diff).
# Re-based 439 → 444 at the N-ary law (pin 05fd2307ff43): the diff adds
# zero trial/final machinery — the +5 are the landing's own new wheel
# fns (infer_fanout_nary and kin, the fanout thunk walk, the named
# render/free-var stages) whose schemes join the pre-existing
# freeze-root divergence class (schemes-are-values, PLAN §7), the same
# channel as the 424 and 439 re-bases one landing over. The next legal
# move stays DOWN; rung 3 (Phase 5.2) dissolves the class.
# Re-based 444 → 451 at mentl where (pin follows the 3.2 landing): the
# +7 are the verb's own new fns (where_badges and kin) joining the same
# freeze-root divergence class; zero trial/final machinery in the diff.
# Re-based 451 → 474 at named effect rows (pin 6768ffac9dfa): eleven
# declared clauses widened (+EnvRead +Intern +Diagnostic — the
# build_declared_row cascade through the parser's fn-type rows), so the
# trial/final comparison surface moved with the semantics — the
# publish-surface channel, fourth application; plus the expansion's own
# new effects fns. Zero trial/final machinery in the diff. The next
# legal move stays DOWN; rung 3 (Phase 5.2) dissolves the class.
# Re-based 474 → 662 at the usage grade (§11 4.2): resolved ownership is
# part of the published scheme, and the mode-paired walk reads CALLEE
# products — so a forward-referenced callee resolves between trial and
# final and the param's grade moves with it (the stamp's named
# order-dependence, now MEASURED at +188 moved schemes on fixed input).
# Zero trial/final machinery in the diff; the publish-surface channel,
# fifth application. The next legal move stays DOWN; rung 3 (Phase 5.2)
# dissolves the class and this order-dependence with it (class-based
# reads are decl-order-free by construction).
# Re-based 662 → 691 at the zip-cliff fix (pin dedfec69264a): zip_with
# rebuilt in the buffer-counter tail form adds zip_with_fill and
# reshapes zip_with/zip/enumerate's bodies — the new and reshaped
# prelude schemes join the freeze-root divergence class (the same
# publish-surface channel, sixth application; zero trial/final
# machinery in the diff). The next legal move stays DOWN; rung 3
# dissolves the class.
# Lowered 691 → 686 at rung 3's B2 (pin 2a09f3c22a4f): the one-way arg
# edge deletes a per-call-site teach-back channel — the arc's
# monotone-down acceptance moving.
# Lowered 686 → 678 at the prereg-row deletion (pin bb3c10c17e3b): the
# skeleton row went bare — the declared-names supply channel closed,
# killing the prereg-mediated eight (self-call/forward-ref charges).
# The remainder is the published-scheme snapshot itself; rung 3
# continues against it.
# Lowered 678 → 453 at the row-half landing: the prereg publish is
# generalize's own floor (the top-row cell unquantified-and-shared), so
# a forward charge chains the live prereg cell and the callee's judgment
# fills it — 225 movers died with the severance. The remainder is the
# grade class (param modes read as values) and the type-sort set (the
# scheme-object rule's territory).
# Lowered 453 → 451 at the worthiness prune (pin 2d3b3ca384a9): two
# family schemes left the mover set with the deleted fns.
# Re-based 451 → 458 at the sig'd poly-recursion landing (pin
# d8142b3b1d98): a fully-sig'd decl's self-call now instantiates the
# prereg quantified scheme instead of chaining the mono cells, so
# sig'd self-recursive fns' judgment — and their published schemes —
# moved with the semantics (the publish-surface channel, seventh
# application; zero trial/final machinery in the diff). The next
# legal move stays DOWN; rung 3 dissolves the class.
# Re-based 458 → 479 at the Mycroft fragment (pin e4f4c2fcaf87): the
# landing's own new wheel fns (poly_capture's arms,
# infer_fn_speculative, the prebind/recheck/replay helpers) join the
# freeze-root divergence class, and every unsig'd decl's judgment now
# runs inside the speculative bracket — the trial/final comparison
# surface moved with the semantics (the publish-surface channel,
# eighth application; zero trial/final machinery in the diff). The
# next legal move stays DOWN; rung 3 dissolves the class.
# Re-based 479 → 480 at the stability belt (pin 58197fecea01): the
# belt's own new fns (the ty_alpha_eq family) join the freeze-root
# class — the new-fn channel; zero trial/final machinery in the diff.
# Re-based 480 → 481 at the nested-fn column swap (pin ae471c6b7fc1):
# neither new projector fn moves (measured — absent from the movers
# line); an EXISTING scheme's trial/final divergence RELOCATED under
# the source restructure, the condemned pass's documented artifact
# (two builds proved the divergence relocates rather than resolves;
# it dies with the one-pass judge). Zero trial/final machinery in
# the diff; the next legal move stays DOWN.
# Re-based 481 → 482 (2026-08-08, the unused-param-width landing): the
# honest +EnvRead widening on spec_wide_twin_names' declared row (the
# emit's env-bucket param read) relocated one divergence — the same
# condemned-pass artifact class as the 481 re-base above; no
# trial/final machinery in the diff. The next legal move stays DOWN.
# Re-based 482 → 483 (2026-08-08, the row-contradiction diagnostic, pin
# d06486727bbb): the meet's new report path relocated one divergence —
# the same condemned-pass artifact class; no trial/final machinery in
# the diff. The next legal move stays DOWN.
# 2026-08-11 holes-column landing: +1 — the new column fns' schemes move
# once between trial and final (the standing channel; dies at rung 3/D8).
# +1 at the slot-home column: resume_upd_pair's scheme moves once (the
# standing trial/final channel; dies at rung 3/D8).
# Re-based 476 → 478 at the delay-depth read (2026-08-12): none of the
# landing's own fns move — measured by building BOTH trees this turn, and
# the named mover set is BYTE-IDENTICAL across them (same sixteen names,
# same A/B fingerprints: process_lowpass's `buffer:rlf`→`buffer:olf` and
# its family). Two EXISTING schemes' divergences relocated under the
# source restructure (infer.mn gains the depth read, so decl order
# shifts), which is the condemned pass's documented artifact — the
# 480 → 481 entry above, measured the same way, and 5.2's ruling that
# two builds refuted one-sided patching because the divergence relocates
# rather than resolves. Zero trial/final machinery in the diff; the next
# legal move stays DOWN; rung 3 dissolves the class.
# 476 → 475 at the concat-spine deletion (2026-08-16): the demand
# analysis lost seven worthiness fns and its accumulator changed shape,
# so decl order shifts and one existing scheme's divergence goes with
# them. No trial/final machinery in the diff — the same relocation
# artifact the entries above measure, moving DOWN this time.
# 475 → 469 at the dep walk's parse deletion (2026-08-17): six dep-walk
# rows tightened to their measured bodies, so six schemes the trial
# published wider than the final no longer diverge — the divergence did
# not relocate here, it was the over-declaration itself. No trial/final
# machinery in the diff; rung 3 still dissolves the class.
# The first read of this said 468, measured on a tree that carried an
# authored `ref` on import_edges; the quiet gate refused that marker
# (inference derives the borrow), and dropping it moved one scheme back.
# 469 is the count of the tree that shipped — §9.11, a banked expectation
# is a hypothesis about the era that banked it.
# 469 → 470 at the singleton-block direct call (2026-08-17): a block of one
# judges in the ROOT's instance now instead of a spawned one, so one scheme's
# trial/final divergence relocated with it. Zero trial/final machinery in the
# diff — the same relocation artifact the 480 → 481 entry above measures, and
# the same ruling applies: the divergence relocates rather than resolves, and
# rung 3 deletes the class. Fixed inputs: same wheel source, same fixture set,
# the only variable being whether a singleton branch spawns.
# 469 → 470 at the result cap's record arm (2026-08-18), and this one is TWO
# RATCHETS IN OPPOSITION, measured both ways on the same landing. The arm maps
# over a record's fields; written as an inline lambda the counts are movers 469
# / anonymity 385, and the anonymity tier convicts it — "a row newly denied its
# decl home", correctly, because the mapped work carries a row. Written as the
# named `cap_result_field` the counts are movers 470 / anonymity 384. A new
# top-level fn is a new scheme and one more trial/final divergence; there is no
# third form. The named one wins because it is the structurally right answer
# and the anonymity conviction is about the code, where this is the same
# relocation artifact the two entries above already rule on — the divergence
# relocates rather than resolves, and rung 3 deletes the class. Fixed inputs:
# same wheel source and fixture set, the only variable being lambda vs named fn.
# 468 → 470 at the compile-context collapse (2026-09-01), the same opposition
# read one more time and settled the same way. Seven lowering routes each
# hand-copied the eight-handler emit tail; `mentl march`'s copy had lost
# lower_handler_stack_ctx and the verb was DEAD — the wheel unable to judge its
# own generation while tools/march.sh, which the board actually calls, stayed
# green. The chain got one home (emit_context) and the nesting order a second
# (compile_context), and those two new top-level fns are the two new movers.
# Both are effect-polymorphic HOFs taking a thunk, which is exactly the shape
# the class is made of: callee_borrow_params answers None when the callee's env
# entry is not yet an arrow, every bare arg reads, and the final's re-judgment
# grades it own. Two HOFs in, two movers out — the 100% rate against a ~0.8%
# baseline is the mechanism showing itself, not noise. Fixed inputs: same wheel
# source and fixture set, the only variable being six chain copies or one.
# 470 → 471 at the severed-proof fix (2026-09-03). free_in_ty's TRecordOpen arm
# collected its row var unconditionally, so a BOUND row — one already carrying a
# residual the graph had proven — was reported free, generalize quantified it,
# and instantiate minted a fresh empty one in its place. Measured on mn-findtag:
# the annotation closed row 24730 and the field access read 24736, six cells
# later, offset unprovable in between. free_in_record_row and its tail walk are
# the two new fns and the one new mover; they apply the rule free_in_row states
# six lines below and this sort had never applied.
# 471 → 470 at the dead-import deletion (2026-09-04): `types` leaving every
# user weave takes one scheme out of the trial/final population with it.
# 470 → 471 at the imports facet (2026-09-04). Its readers are new top-level
# fns and one of them moves, the same arithmetic every added fn pays in a
# tower that shifts ~0.8% of schemes. The other three ratchets this landing
# tripped were FIXED rather than raised: the ref markers came off (the quiet
# gate is right that annotating around inference is the failure), the two
# returned lambdas became hole-products under the Stage Law, and the
# backticked names that resolved nowhere became prose.
# 471 → 472 at the unreferenced facet (2026-09-04). Eight new fns across
# query.mn and pipeline.mn, and the tower shifts ~0.8% of schemes, so +1 is
# the arithmetic any added declaration pays here — the same +1 the imports
# facet paid one pin earlier for the same reason. No condemned machinery was
# touched or improved; rung 3 is what takes this to zero.
#
# 472 → 476 at the performs facet (same day). Nine added declarations — six
# in query.mn, three in pipeline.mn. The per-fn cost is not a constant: the
# imports facet paid +1 for two fns and the unreferenced facet +1 for eight,
# where this pays +4 for nine, because what moves a scheme is whether the
# trial pass judges a fn before its final and that turns on forward
# reference, not on count. Recorded rather than smoothed over. Again no
# condemned machinery was touched; the tower's deletion at rung 3 is what
# takes this to zero, and every facet landing that adds fns will keep
# nudging it until then.
#
# 476 → 472 at the Delta deletion (same day): gradient_delta.mn's 407 lines
# left the weave, and the tower has that much less to override. The number
# is deterministic, so this is the value and not a sample — tightened rather
# than left slack, because a ceiling above the measurement is exactly the
# room a regrowth hides in.
#
# 472 → 474 at the orphan-claims facet (same day): eight added fns across
# query.mn and pipeline.mn. Same family as the three facet landings before
# it, and the same non-constant per-fn cost — what moves a scheme is
# forward reference, not count. No condemned machinery touched.
#
# 474 → 476 at the signature-column landing (2026-09-07): the same family
# again, and the same per-fn cost. The arc added ~15 fns across types.mn,
# env.mn, lower.mn and wasm.mn (the enumeration entry's edge producers, the
# settled-ABI readers, and the lambdas the anonymity tier asked be given decl
# homes) while DELETING the param-width ladder and the 30-arm
# find_local_handle_expr family. What moves a scheme is forward reference,
# not count, so a net line-count fall does not imply a mover fall. NO
# CONDEMNED MACHINERY WAS TOUCHED — the two-pass tower is untouched by this
# landing, which only changed where emit reads a width from. The number goes
# to zero at rung 3's dissolution, whose real DEP this landing was built to
# remove (`Hβ.infer.live-cells-need-one-settled-signature`).
# 2026-09-07: 476 -> 435 at the module-blind-parse landing. Not a movers fix;
# the per-module parse changes what the trial and final each see, and the
# class fell out. The condemned pass still owns the rest.
# 2026-09-15 · 435 -> 433 · at the diagnostic-address landing (d8d86cf5), which
# was not about movers at all — diag_line took a module param and
# diag_report_at was born, and two schemes stopped diverging. The FELL notice
# fired on that run and was MISSED, because the run's output was read through a
# `grep ✗` filter: the same defect this session has been documenting in the
# gates, committed by the reader instead of the gate. Read the whole verdict.
# AND IT CORROBORATES THE REPRICING ABOVE: movers moves on incidental
# decl-shape changes and did not move at all when liveness shipped (470 → 470,
# infer.mn:1054). It is a size gauge for the condemned pass, not a road to
# zero — deleting the pass is what reaches 0
# (`Hβ.infer.judge-once-per-scc`).
# 2026-09-17: 433 -> RETIRED, by the entry's own words — "deleting the pass
# is what reaches 0." The second judgment is deleted (infer_program_once is
# the one walk), so there is no trial for a final to override, no line for a
# gauge to read, and no key: a ratchet held at zero over a deleted instrument
# is a mute gate. The regrowth alarm is structural — a second pass would
# have to be written back into infer.mn.

# The effect seam: filesystem impl calls outside the handler that owns them.
# MEASURED 24 before this landing and 13 after (both read from the artifact,
# the pre-number taken by stashing the fix and re-running the tier — not
# derived from counting edits). Eleven conversions: march_run's whole file
# sequence, main's emit route, and mcp's gate, each provably under a
# `~> wasi_filesystem` install, so the ops were always reachable and the
# impls were reached past.
#
# 13 → 11: mcp_run's two mkdirs moved INSIDE its own `~> wasi_filesystem`
# bracket, where the op is reachable. They had sat two lines above the
# install, so the bypass was the only spelling available.
#
# 11 → 8: the substrate question got measured instead of guessed.
# tests/micros/mn-handler-extent-tail-loop.mn asks whether a handler's
# extent survives a self-tail-calling loop, and it does — the state
# accumulates across iterations too (1+2+3+4 = 10). So space_run installs
# wasi_filesystem over its infinite accept loop and the file server performs
# the ops. Two more converted where the row ALREADY declared Filesystem and
# only the call site had not caught up: driver_warm_persist's mkdir and
# persist_write, whose declared row said Filesystem while its inferred row
# said WASI. That one reverted (see below); driver_warm_persist stayed.
#
# persist_write was converted and REVERTED, and the revert is the finding.
# The frontier's persist shadow leg refused it: `fs_write_file` answered
# E_MissingVariable in the subset link, because the Filesystem effect is
# declared in src/types.mn and lib/persist.mn must compile standalone. So a
# lib/ module cannot perform this effect at all — the impl there is forced,
# not lazy. That is a floor under this ratchet, not a site awaiting work.
#
# The remaining 8: battery_libs' four reads, main's battery loop read,
# dsp/cfc's read, image_resume's, and persist_write's structural one. All
# but the last sit in a fn declaring `with ... WASI` with no Filesystem
# handler in its chain, so the op is unreachable and the impl is the only
# thing that resolves; they convert when their callers install, a per-route
# reading rather than a sweep. Whether the lib/ floor can fall at all is the
# effect's own home question — moving Filesystem out of src/types.mn is a
# different landing than converting call sites.
#
# Monotone DOWN. A rise means a caller reached past the op, which keeps the
# behaviour and drops the effect from the row INVISIBLY: the impls charge
# WASI while the capability the handler names is Filesystem, so
# `with !Filesystem` went on holding over routes that created, wrote,
# renamed and unlinked files.
# 2026-09-07: 8 -> 0. driver_manifest hashed each module by RE-READING its
# file; the text rides the entry now, so it hashes what it was handed and the
# last filesystem calls outside the handler that owns them are gone.
#
# 2026-09-12 · 0 -> 9 · AND THE ZERO WAS NOT A MEASUREMENT. This gate counts
# by parsing the refs answer for '^  at <module>:', and at pin 0268bb1e
# (2026-09-08, "the parse enters the module") the refs facet was downgraded to
# render a BARE span — `at 3090:3` — in the very commit where its census and
# debt siblings GAINED module_name_of_handle. A bare span cannot match
# [a-z_/]+, so from 09-08 the count was structurally zero: not clean, BLIND.
# It read green for four days. This is §11 tripwire 4 in its second form —
# the first was a gate that stopped being REPORTED, this is one that stopped
# being ABLE TO ANSWER — and it was found only because refs-of was given its
# module back (the same landing as this line).
#
# The 9 it sees, triaged at the artifact, every one a site where a declared
# `!Filesystem` would falsely prove absence over a real file operation:
#   persist:162  the persist_to_disk handler's OWN arm — legitimate; this
#                allowlist (pipeline|io) predates that handler's landing.
#   persist:152  persist_write — real, already named Hβ.persist.branch-world-tag
#   persist:93   image_resume reads the wire past the op — real
#   main:1732 ×4 battery_libs concatenates four lib sources directly — real
#   main:1866   the micro battery reads each fixture directly — real
#   dsp/cfc:270 read_recording, a library reaching the substrate — real
# Eight real bypasses accumulated while nobody could see them. The ceiling is
# the MEASUREMENT, not a tolerance: it is monotone down from here and the
# landing that drives it to zero is the next filesystem-seam arc. Raising it
# again requires the same enumeration this entry carries.
# 2026-09-17: 9 -> 8. persist's write became ONE operation with one home —
# "this path ends up holding exactly these bytes, or it does not exist" is a
# single filesystem question, so fs_write_image_impl (lib/io.mn) owns the
# open/write/write/close/unlink rather than persist reaching past the seam five
# times for it. The reach that remains there is the image READ, and the arc
# that drives this to zero is the layering one the bypasses all share: the
# Filesystem effect is declared in src/types.mn, so a lib/ module cannot
# perform it and still compile standalone. Move the decl to io.mn beside its
# impls and every one of these becomes an op the handler can see.
# Held at the measured 7 the landing after — the board read 7 the night the
# entry above was written and the ceiling was left at 8 to spare a gate cycle.
fs_impl_bypass_max: 7

# 2026-08-07: the PER-MODULE ratchet (PLAN §11 Phase 3.5's first half) —
# E_MissingVariable across every src module's SOLO check, monotone DOWN.
# Seeded 36 after the two one-line import fixes (verify→graph+io killed
# 12+1, format→parser killed 5; the sweep measured 53 before them). The
# residual 36 IS two architectural seams, not under-imports: the
# infer→pipeline handler pair (env_handler/intern_view installed in
# infer.mn:2310 but declared a layer up — the declarations relocate DOWN,
# ~20 violations across nine closures) and the mcp→main verb grammar
# (parse_cli_args + VerbSpec shared across two transports but declared
# at the entry — the grammar relocates to a home both import, 16
# violations). The frontier's solo-sweep leg enforces this ceiling; it
# reaches 0 when the seams close and the per-module check becomes the
# drift-catalog retirement gate.
# RATCHETED DOWN 36 → 16 at the handler relocation (env_handler + the
# env machinery → src/env.mn; intern_table/intern_view + intern_probe →
# src/intern.mn; infer imports both, healing all nine closures at once).
# RATCHETED DOWN 16 → 0 at the verb-grammar relocation (EntryVerb, the
# VerbSpec table, parse_cli_args + address/target helpers → src/cli.mn;
# main and mcp both import it — one grammar, two transports, one home).
# ZERO IS THE FLOOR AND THE CONTRACT: every module's solo check resolves
# every name through its own declared imports. Per §11 3.5 this gates
# the drift-catalog retirement.
# INTERROGATED 2026-09-15 and KEPT — the measurement is recorded so the
# next session does not re-derive it. The day E_UseAfterMove was armed and
# use_after_move_max retired, this key was the obvious next candidate: it
# reads ZERO, and the thing it counts (E_MissingVariable) ALREADY refuses,
# which looks exactly like a counter standing beside a proof.
# It is not. E_MissingVariable refuses the program the wheel actually
# LINKS — one concatenated blob in which every name resolves whether or
# not its module declared the dep. This sweep checks each module ALONE,
# which is a judgment the wheel never performs on itself, so the zero here
# is not a held invariant but a genuine measurement of something otherwise
# unobserved. That is §11 tripwire (3) — the board is blind to what the
# wheel never does — and lib/** joining the sweep in 2026-08-16 found 20
# unresolved names the very first run, which is the proof that retiring it
# would delete a real channel rather than a duplicate. Its honest
# retirement is the per-module env overlay (§7: "CLOSED at entry, OPEN
# per-module … the overlay is the stamped second half",
# Hβ.driver.per-module-env-overlay), after which the link itself judges
# per module and the sweep has nothing left to discover.
solo_violations_max: 0

# 2026-08-07: the ANONYMITY ratchet (PLAN §11 Phase 2.5, second half) —
# the census tier's convictions on the wheel link, monotone DOWN. Two
# keys, not one, because the classes fall differently: an eta-wrapper
# dies by passing the name that already exists (MachineApplicable); an
# effectful lambda dies by a named stage gaining the row's decl home.
# Raw CsAnonymous is NOT ratcheted — its pure-local majority is the
# vocabulary the tier itself declares silent. Seeded at the pin
# 62542a59bf94 measurement: eta 29, effectful 394 (of 555 anonymous
# total — the convictions are 423 of them; the graph's exact read,
# correcting the stamp's ~136 text-shape eta estimate). Each key dies
# if its class reaches 0 and stays.
# Lowered 394 → 383 at the prereg-row deletion (pin bb3c10c17e3b): the
# declared-names carry died and the new pin's judgment re-classes the
# prereg-fed lambdas' rows — eleven convictions gone with no lambda
# authored or renamed, the census reading the graph, not the text.
# Lowered 383 → 381 at the worthiness prune (pin 2d3b3ca384a9): the
# deleted family carried two convicted lambdas.
# Re-based 381 → 382 at the debt facet (pin a90453e3e444): the site-list
# map lambda over the hoisted seams — the byte-identical shape of its
# sibling render_ref_spans in the baseline above it; the pair retires
# together when the seam-threading render gains its named stage.
# Re-based 382 → 383 at the row-contradiction diagnostic (pin
# d06486727bbb): the pair-law any-lambda inside
# report_row_contradictions — the byte-identical shape of
# absent_minus_present's own filter lambda in the baseline (both close
# over the pair law's !Mutate row); the pair retires together.
# Ratcheted 29 → 28 at the trio deletion (pin 54c97b0f7220): oracle's
# `(pos) => candidates_at(pos)` eta-wrapper became the ??-suspension
# `candidates_at(performed, ??)` — the Stage-Law form, no wrapper at all.
# 2026-09-08: 28 -> 11. Seventeen eta-wrappers deleted — each was
# `(x) => f(x)` around a plain top-level fn, so the name was passed bare.
# The remaining ELEVEN were not style, they were BLOCKED: a bare non-fn
# binding used as a function value compiled with ZERO diagnostics and then
# either trapped or emitted `(global.get $<name>)` for a global that did not
# exist, the assembler objecting first.
# 2026-09-09: 11 -> 3, in TWO landings, because the boot compiles m2 and the
# wheel cannot write a form the boot does not carry (measured: bundling both
# made m2 fail to assemble at wat:99217). The fix landed first
# (Hβ.emit.nonfn-binding-as-function-value — a bare name is the parameter
# product with every field a hole, so it reifies through the partial mint),
# then the eight unblocked wrappers came out against the new boot: four
# constructor wrappers in cli.mn, three `map((v) => TVar(v), …)` in infer.mn,
# and `map((h) => interrogate_at(h))` in eight_loop.mn.
# 2026-09-10: 3 -> 0, at pin f862677e5959751a. The last three were one shape
# and a different blocker: `map((x) => process(x), buffer)` three times in
# lib/dsp/processors.mn, where `process` is declared by lowpass, biquad AND
# envelope. A reified op resolved its handler from the live world chain by
# HANDLER name, so an ambiguous op had no single name to resolve and no
# single arm to call. The one-walk collapse keys that walk on the OP, so
# ambiguity stopped being fatal and the three wrappers came out bare — a
# CONSEQUENCE of the walk, never a target, and specifically not the
# rewrite-as-named-wrappers move that would have laundered the same
# redundancy past this very count.
# ZERO IS NOT A RESTING PLACE, it is a refuse-contract: the next eta-wrapper
# to appear in the wheel raises this and the gate refuses the pin. What the
# count MEANS at zero is that no named fn in the wheel hides behind a lambda
# that only forwards to it.
# 2026-09-23: `why-coordinates` was declared RED here from 2026-09-06 and
# RETIRED by proof: a declaration's reason stopped copying its own name and
# span and became an edge to the declaring node, whose address carries the
# file (`why gain` → `declared at tests/frontier/mn-where-badges:8:1-8:15`).
# 2026-09-23: `open-rows-through-lambdas` and `record-rest-over-open` were
# declared RED here the same day and RETIRED by the record row landing: two
# open rows meet at one fresh var (Rémy) instead of absorbing each other as an
# "assumed" remainder, and a record pattern reads its layout at emit under
# the twin. `record-closed-lacks-field` stayed RED one landing longer, on
# E_TypeMismatch not refusing the executable; it retired when every error
# became a refusal (`diag_refuses` deleted into the severity).
# 2026-09-23: the rows design's third refuter — an instance reaching an install
# through a HOF parameter is never unified with the handler's (an Int payload
# called as a function, runtime trap), and a module-level `let` performs with
# no frame (out-of-bounds fault in the initializer). Same retirement.
# `install-over-hof-param-payload` RETIRED 2026-09-24 with the speculation
# landing (Hβ.infer.speculation-leaves-a-handle-standing), measured passing
# through the march's m3.
frontier_expected_red: module-let-performs
# 2026-09-23, the fourth refuter: a task handed to `spawn` performs E inside a
# `!E` body and checks clean under BOTH schedules, so the gap is the task's row
# never reaching the install, not only the host boundary. Same retirement.
# The SEQUENTIAL schedule's leg RETIRED 2026-09-24 with the same landing; the
# parallel leg still checks clean, so the host boundary is the half left.
frontier_expected_red: spawn-thunk-parallel
# 2026-09-23: AN INSTALL SOMEWHERE IS CREDITED FOR A PERFORM IT DOES NOT
# COVER. `bad()` performs E under `~> h` on its left operand and through an
# uncovered HOF call on its right; nothing declares a row, E reaches main's
# root, the gate clears it because `h` is installed somewhere, and the
# executable faults out of bounds in ev_declaring_node. The contract is the
# refusal (tests/frontier/mn-refuse-uncovered-sibling.mn); retires when the
# gate discharges a performed name only by an install that reaches it
# (Hβ.effects.reachable-perform-with-no-install-compiles).
frontier_expected_red: uncovered-sibling-refuse
# 2026-09-23: A `<~` STATE ELEMENT'S INIT IS DISCARDED. `accumulate(5)`
# starts its register at zero — tests/frontier/mn-accumulate-init.mn answers 2
# where 7 is written, no diagnostic. The site reads the depth and nothing
# else; retires when it reads the element whole and the register is born
# holding its init (Hβ.dataflow.state-element-is-read-whole).
frontier_expected_red: accumulate-init
# 2026-09-23: A GENERIC CONSTRUCTOR FIELD INSTANTIATED WIDE IS READ AT WORD
# WIDTH inside a polymorphic fn: `Held(a, Int)` built with a Float, matched in
# a fn generic in `a`, reads `tag` from the Float's bytes — 0 through `run`,
# the high word through `test`, no diagnostic. Right in `main` and through a
# generic constructor fn; wrong on the boot before patterns became nodes too.
# Retires when the read is right (Hβ.value.generic-payload-read-at-word-width).
frontier_expected_red: generic-payload-wide-read
# 2026-09-23: A PIPE COMPLETED INSIDE A SPLICE SHOWS AN ADDRESS. `"{xs |>
# join(sep())}"` renders the partial call's closure where the joined string
# belongs; the pipe node judges String and the show reads the lowered call's
# handle instead. Found when a formatter render wrote `Sample(99972)`.
# Retires when the show reads the node it shows
# (Hβ.lower.pipe-completion-carries-the-stage-handle).
frontier_expected_red: pipe-in-splice-show
# 2026-09-18: `==` on an operand whose type is still a variable at emit (a
# handler arm over quantified op parameters — no call-site twin reaches it)
# emits i32.eq on two heap addresses; tests/frontier/mn-eq-in-arm-pointer.mn
# answers 1 for two byte-equal Strings with no diagnostic. Declared RED the day
# it was measured; retires when the eq leaf refuses the unresolved operand
# (Hβ.emit.eq-on-unresolved-operand-is-pointer-eq) and the gate SAYS so.
frontier_expected_red: eq-in-arm-pointer
# 2026-09-23: a resume inside a lambda whose caller keeps computing with the
# lambda's answer is taken as the arm's tail — 13 where 40 is written. The
# arm body and a fn nested in it are read exactly now (non-tail → the real
# continuation); the lambda keeps the assumption because walking it as
# non-tail would put correct programs (a thunk the caller returns unchanged)
# on the real-k path, which floors a perform off the k1 spine.
# Hβ.lower.resume-through-a-lambda-is-taken-as-the-arms-answer.
frontier_expected_red: resume-through-caller
# 2026-09-25: A REFERENCE RESOLVED BY NAME IN THE WRONG SCOPE. Two refuters of
# the resolution-at-birth design each found programs the medium reads by NAME
# where the language says a reference means a BINDER, every one re-run by hand
# the same day. Wrong answers with a clean check: a parameter default and a
# handler's state init or config default lowered in the caller's frame (243,
# 101 and 51 where 21, 6 and 2 are written), a block's lets walked backwards
# (a trap where 56 is written), a nested fn's later sibling read as its
# top-level namesake (110 for 16), a feedback prior shadowing the local that
# shadows it (1 for 101), a parameter spelled `hash` hijacked by the
# structural leaf (41 for 42); and verdicts that change with source order or
# with the spelling of a parameter, including a literal in parameter position
# read as `_`. All retire with Hβ.resolve.reference-is-an-edge.
frontier_expected_red: resolve-default-callee-scope
frontier_expected_red: resolve-handler-init-scope
frontier_expected_red: resolve-config-default-scope
frontier_expected_red: resolve-block-let-order
frontier_expected_red: resolve-nested-fn-letrec
frontier_expected_red: resolve-feedback-prior-shadow
frontier_expected_red: resolve-param-named-hash
frontier_expected_red: resolve-source-order-verdict
frontier_expected_red: resolve-default-edge
frontier_expected_red: resolve-literal-param
# The same arc's correct programs the medium refuses to build: mutually
# recursive nested fns (E_MissingVariable), a local named `abort` beside a
# refutable `let` (the desugar's name captured), a parameter spelled like a
# top-level fn (six E_TypeMismatch through a phantom call edge), a lambda
# closing over a feedback prior (`unknown local` at assemble), and a
# top-level destructuring `let` (`unknown global` at assemble).
frontier_expected_refused: resolve-nested-fn-mutual
frontier_expected_refused: resolve-desugar-hygiene
frontier_expected_refused: resolve-alpha-renaming
frontier_expected_refused: resolve-feedback-capture
frontier_expected_refused: resolve-toplevel-destructure
# 2026-09-25, the second refutation round: every place the compiler asks what
# a SPELLING means is the same re-derivation. Wrong answers with a clean
# check: a shadowing let or match-arm binder overwrites the outer local's
# register (4 for 3); a nested fn's default traps (called by name after its
# declaration left the env, and unable to read its capture); a parameter
# spelled like a top-level fn with defaults calls that fn (trap). Refusals
# that do not fire: a nested fn generalized over its capture runs; a
# destructuring or braceless let's annotation is dropped; a handler config
# default is judged before the fn it calls; a config annotation is dropped;
# `let 0 = x` is irrefutable; `fn f(0)` loses its parameter (the wrong class
# refuses). All but fanout-grouping retire with Hβ.resolve.reference-is-an-edge.
frontier_expected_red: resolve-shadow-block
frontier_expected_red: resolve-shadow-arm
frontier_expected_red: resolve-nested-default
frontier_expected_red: resolve-nested-default-capture
frontier_expected_red: resolve-param-shadows-fn
frontier_expected_red: resolve-nested-generalize
frontier_expected_red: let-annotation-destructure
frontier_expected_red: let-annotation-braceless
frontier_expected_red: handler-config-default-order
frontier_expected_red: handler-config-annotation
frontier_expected_red: let-literal-refutable
frontier_expected_red: literal-fn-param
# Correct programs the medium will not build: a shadow of another width
# (`unknown local`), a generic default at two types, a parameter spelled
# like an effect op (does not assemble) or `len` (typed as the sequence op),
# a module-level `abort` capturing a desugar, a state update's target found
# by name, `(a)` read as a one-tuple, and an author-parenthesized fanout
# flattened into its parent (the parser's accretion, fixed on its own).
frontier_expected_refused: resolve-shadow-width
frontier_expected_refused: resolve-default-instantiates
frontier_expected_refused: resolve-param-named-op
frontier_expected_refused: resolve-param-named-len
frontier_expected_refused: resolve-toplevel-abort-hygiene
frontier_expected_refused: resolve-resume-update-target
frontier_expected_refused: pattern-paren-grouping
frontier_expected_refused: fanout-grouping
# 2026-09-23: FIFTY CROWN CRUCIBLES THE PINNED BOOT FAILED, written by four
# adversarial refuters of the rows design and each seen RED through the boot
# the hour it was written. 2026-09-24: FORTY-TWO hold through the rows
# landing's cells (Hβ.effects.rows-are-propagated-cells) and their
# declarations are deleted. The eight below still fail, and each names its
# carrier:
# - a bound on a parameter forwarded across a recursion group, and a handler
#   config through one;
# - fn payloads in a nested nominal and in a recursive type;
# - a nominal record built by the call form, and its pure sibling refused.
# The seventh and eighth — a parameter bound crossing a generalized callee,
# and through a lambda — hold since the speculation landing
# (Hβ.infer.speculation-leaves-a-handle-standing), measured through the
# march's m3; which of its four closures carried each is not isolated.
crown_expected_red: leak-group-forwarded-param-ceiling
crown_expected_red: leak-handler-config-through-group
crown_expected_red: leak-nested-type-payload
crown_expected_red: leak-recursive-type-payload
crown_expected_red: leak-nominal-record-ctor-call
crown_expected_red: sound-nominal-record-sibling
# 2026-09-18: `eq-polymorphic-sum` was declared RED here for one landing and
# RETIRED by proof in the next — the whole life of an expected-red entry, in a
# day. It read: `==` on a POLYMORPHIC sum compares its payload by ADDRESS,
# because `fold_sig` folded `TName(n, args)` to the bare name so one
# `$eq_nOption` served every instantiation, and `variant_specs_of` answered
# with the constructor's DECLARED payload type. The sig folds its arguments
# now and the specs ground them, so the leaf calls its argument's own helper;
# the gate's other direction refused the stale declaration the moment it
# passed, which is what that contract is for.

# record_pattern_open_max — record patterns whose RECEIVER row is open, so
# their field offsets come from the pattern's own index rather than the
# receiver's full sorted field set. Born at ZERO (pin 4ce9914b7360), which
# is a contract rather than a tolerance: the wheel's two sites were open
# until their receivers were annotated one pin earlier, and they were
# correct only by an author's promise the graph could not check — a
# complete pattern over an open row is structurally identical to a partial
# one. Seen RED by falsification: removing one annotation moves the count
# 0 → 1 with the site located. Class: Hβ.lower.record-pattern-param-receiver.
loop_scaffold_refs_max: 3

# 2026-08-17: the SUGAR VOCABULARY contract — the prelude names the five
# desugar-capable modules mint as literals, which is the seed set
# Hβ.driver.link-is-reachability needs and a name-keyed contract nothing
# else checks. EXACT, not a ceiling: a drop is a prelude rename breaking a
# desugar path, a rise is a new mint nobody reviewed. Measured 44 with
# str_literal_5 (an identity relic) already deleted out of it; the spliced
# names were shown compiler-synthesized, so the literal scan sees all of it.
# 2026-09-10 · 43 -> 44 · RE-DERIVED, and the seed follows the set because
# the set is what the emit actually mints. The world-chain walk's minted trio
# changed shape: `ev_perform_node` and the effect-only `world_find_from` are
# DELETED and `ev_declaring_node`, `world_declaring_from` and `node_arm_at`
# replace them — two names out, three in, because asking whether a handler
# DECLARES an op (rather than merely covering its effect) is one read the old
# pair had no home for. The +1 is that read.
# 2026-09-11 · 44 -> 41 · RE-DERIVED, and the seed follows again — the same
# trio, one generation later, going the other way. `ev_declaring_node`,
# `world_declaring_from` and `node_arm_at` left the desugar vocabulary at pin
# 0e79bdbd, when the world-chain walk moved OUT of lib/memory.mn and into the
# emitted preamble beside $world_push: emit stopped minting their names as
# demands, so they stopped being a name-keyed contract with lib/ at all. The
# contract breach was real and this baseline move was MISSED at that landing —
# found here by the gate, bisected to that pin, and the set diffed to exactly
# those three. 41 SURVIVES the crossing-generation deletion that follows the
# next repin: the three fns still sitting in lib/memory.mn are already out of
# the intersection, so removing them moves nothing. The number falls to zero
# when Hβ.driver.link-is-reachability lands and an emit-minted name demands
# its defining module the way an import does.
desugar_vocabulary: 41

# 2026-08-08: the DRIFT-SHAPE ratchet (PLAN §11 5.6's absorption,
# enforcement half) — the three absorbed drift modes counted by the
# census shapes on the wheel link, replacing their bash rows (which
# retire in the same commit: the grep dies; the projection + ratchet is
# the check, the mode-33 precedent). RED-seen at wildcard_zero_max: 8 /
# failure_mask_max: -1 / print_in_report_max: -1 before these ceilings.
# wildcard-zero seeds at the measured 9 — the wheel's DOCUMENTED
# sentinels, each carrying its inline reason at the site (ty_handle_of's
# ground-type zero and kin); a rise is a NEW masked case. The structural
# detector sees through formatting the line-anchored regex could not.
# failure-mask and print-in-report seed at their measured ZERO — a bug
# is zero or blocking, and a print inside report(...) corrupts WAT
# stdout (§9's bug list); zero is the floor and the contract.
# ── the SELF-COMPILE's PEAK RSS (tools/march.sh, the m3 leg under GNU time) ─
# The one line in this file that was a bare number with no argument beside it,
# in the file whose whole point is that a bound and its justification live
# together. Written 2026-09-21, when the gate started deciding by noise.
#
# THE MEASUREMENT THAT FORCED THE RAISE, and it is about the INSTRUMENT, not
# about a landing: two marches of BYTE-IDENTICAL source, minutes apart, took
# min-of-3 readings of 999,400 KB and 1,000,064 KB. A 664 KB spread — 0.066% —
# straddling a ceiling of 1,000,000. The march already re-measures three times
# and convicts on the minimum, and even the minimum flipped. A gate whose
# verdict changes without its subject changing is not measuring its subject.
# So the headroom is set from the jitter: 1,010,000 is ~1% over the highest
# min-of-3 reading and ~15x the observed spread, which still convicts any real
# regression (a judgment regression is tens of MB — the second pass was 1.3 GB)
# while refusing to convict the instrument.
#
# THE RAISE IS NOT THE FINDING. The number has gone 993,832 -> 996,920 ->
# 998,596 -> ~1,000,000 across four pins, and NONE of those landings was about
# memory. That creep is the per-decl arena's absence (PLAN §5.O layer 3,
# Hβ.perf.per-decl-arena): 98% of the judgment's heap is scratch that nothing
# reclaims, so every landing that mints a little more pays it forever. This
# ceiling's job is to FALL when the arena lands — by roughly two thirds, per
# §5.O's own accounting — and a ceiling that only ever rises to meet a breach
# has stopped ratcheting. Raising it is an explicit in-commit act, which is
# what this paragraph is; the next move on this line should be down.
#
# AND IT WENT DOWN (2026-09-21, the same day): 1,010,000 -> 936,000. The
# breach that refused a repin at 1,069,472 KB was a fifteen-line render edit,
# and attributing it built the instrument this file never had — `mentl <file>
# cost` ranks the judgment's heap by decl — which named ONE two-line function
# in format.mn's render cycle carrying 341MB, 40% of the whole judgment. The
# cause was the occurs check walking a DAG of shared row vars as a tree, once
# per path (graph.mn, occurs_in's header). With a visited set the m3 leg
# measures 925,780 KB against the prior pin's 998,948, and this ceiling is set
# ~1% over that reading, the same jitter margin as before. The arena's fall is
# still owed on top of this one.
#
# 936,000 -> 1,000,000 (2026-09-23), attributed rather than absorbed. The
# judgment's heap moved 481,206,424 -> 520,191,416 bytes (m2's line against
# m3's, one march) for two things this landing added on purpose: every
# sub-pattern is a node with its own cell (253,359 -> 264,590 nodes minted
# on the wheel, `mentl src/main.mn cost`), and every match is judged once
# for NESTED coverage, which is what found three int-tag matches in
# lib/lists.mn that trapped on an unwritten tag with nothing said. The
# per-decl instrument shows the rise spread thin — the widest matches
# (`emit_expr`, `infer_expr`, `lower_expr_body`) about 12% each, no single
# decl carrying it — so there is no two-line culprit to delete this time,
# unlike the occurs check above. The one waste the read did find (a
# constructor pattern padded with a fresh list of wildcards when its count
# already matched, src/coverage.mn `at_arity`) is removed and measured
# (523 -> 520MB). Four marches' min-of-3 readings ran 978,080 to 990,764
# KB; the ceiling is ~1% over the highest, the same jitter margin as the
# two entries above. The arena's fall is still owed, and it now owes this.
#
# 1,000,000 -> 1,011,000 (2026-09-23), and the rise is mostly the ceiling
# having sat inside its own noise. Named constructor fields, both steps,
# moved the judgment's heap 524,737,288 -> 526,248,488 bytes (+1.5MB, 0.29%,
# the m3 leg's heap line at each pin), and the min-of-3 peak moved 994,540
# -> 999,744 -> 1,001,100 KB across this landing's three marches — while
# single reads within one march spread up to 1,015,256. The prior ceiling
# was set ~1% over the then-highest min-of-3, and the wheel reached it with
# a 0.3% heap rise, so this one keeps the same ~1% margin over today's
# highest. It is a cost the arena's fall owes, not one this landing removes.
#
# 1,011,000 -> 1,021,000 (2026-09-23, the named-fields sweep's second tier).
# The heap line is the measurement here, because it is byte-deterministic and
# the peak is not: HEAD's source and this tree, marched back to back through
# the same boot, read 529,875,960 -> 530,101,848 bytes (+225,888, 0.04%).
# The peak's three re-reads of this ONE tree spread 1,019,168 / 1,012,460 /
# 1,011,300 KB, 7.8MB, against HEAD's single 1,004,500, so the 300KB breach
# is inside the reading's own spread and not a rise the source caused. Same
# rule as the two entries above: ~1% over the highest min-of-3. The next
# ceiling should be on the heap line itself, which cannot be wrong by a
# coin flip, and the peak kept as the looser sanity bound
# (`Hβ.tools.cost-ratchet-reads-the-heap-line`).
#
# 1,021,000 -> 1,300,000 (2026-09-24, the rows landing), and this raise is a
# DEBT, not noise. The judgment's heap moved 548,585,424 -> 745,370,608 bytes
# (m2's line against m3's, one march); the m3 leg's own `rows:` line puts
# 150,137,920 of it on the effect rows (instantiation 26.5MB · link 90.5MB ·
# charge 19.1MB · union 14.0MB), and the min-of-3 peak reads 1,281,792 KB.
# What it bought: forty-two crown crucibles that were false absence proofs or
# false refusals in the pinned boot now hold (crown 150/0/8), a nested record
# literal no longer evaluates to its INNER record (a silent wrong in every
# program, tests/micros/mn-record-nested-literal.mn), and a handler instance's
# row argument is renamed per use and met by the flow its op gives it.
# What was measured on the way, so the next move starts from it: the rise
# had been 94MB in ONE decl (`emit_expr`, rows 89.7MB) until a growth began
# carrying its delta to readers instead of making each re-walk its source's
# every path (graph.mn RowWake); `mentl <file> cost` now shows each decl's
# row bytes beside its heap, which is how that was found. Two probes were
# refuted: equating instead of linking an install's row arguments (cost
# unchanged), and leaving a recursion member's own frame unrenamed (cost x4
# and a leak). The remainder is the per-link fan-out to a large frame
# cell's readers — `Hβ.effects.row-link-fans-out-per-call` owns it, and the
# ceiling comes back down with it.
# 1,310,000 the same day, a MARGIN correction and not a rise in the work: the
# 1,300,000 line was set about 5MB over one reading, and three reads of one
# unchanged m4 leg spread 1,303,936–1,305,928 KB. Across the fmt gate and the
# tail-call walkers the deterministic heap line moved 756,308,728 →
# 756,942,040 B (+0.08%). The ceiling to ratchet is that line
# (`Hβ.tools.cost-ratchet-reads-the-heap-line`), which RSS jitter cannot move.
#
# 1,500,000 (2026-09-24) — DEMOTED TO A CRASH GUARD, as the banked design
# said it should be. The exact cost ratchet is src/board.mn's JudgmentHeap
# bound now: the weave judgment's high-water line, byte-deterministic, read
# by `mentl verify` at the repin and at every commit, with a ±0.5% band that
# a comment's allocation cannot cross and a real rise cannot hide in. This
# line stays for what that one cannot see — lowering, emit, the WAT buffer —
# and is set far enough over the ~1.3GB it reads that kernel jitter never
# refuses a repin again; it catches a process that is about to be killed.
selfcompile_peak_kb_max: 1500000

# ── the compile's CONCURRENCY WIDTH (tools/thread-gate.sh) ────────────────
# Guest OS threads the compile creates, counted at clone/clone3, as the
# DELTA between a 61-declaration program and a 1-declaration one. The delta
# and not an absolute: wasmtime's own host threads are identical in both and
# cancel, while guest layer-branch spawns scale with the branch count — so
# this ratchet measures US and stays true across runner upgrades, where an
# absolute ceiling would go red on somebody else's implementation detail.
#
# The line above ratcheted the MEMORY the 433-thread defect cost. Nothing
# ratcheted the threads themselves, which is why judge_window = 1 could sit
# beside a spawn-per-branch for ten days with the whole board green — the
# gate that would have caught it on day one is this one, and it did not
# exist. Zero is the honest floor today (measured: 5 clones at 1 decl, 5 at
# 61, both host overhead) because a singleton judge block runs BrDirect.
#
# WHEN PHASE 9.2 RESTORES judge_window = 8 THIS IS RE-BASELINED, NOT DELETED:
# the number becomes the width the parallel walk is supposed to have, and the
# gate goes on answering the same question one octave up. Seen RED at ceiling
# -1 before it was trusted.
judge_spawn_delta_max: 0
