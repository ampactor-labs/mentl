import io

effect Alpha { a_op() -> Int }

effect Beta { b_op() -> Int }

type AB = Alpha + Beta

handler ha { a_op() => resume(1), }

handler hb { b_op() => resume(2), }

fn both() with AB = a_op() + b_op()

fn main() = println(int_to_str({ both() } ~> ha ~> hb))
