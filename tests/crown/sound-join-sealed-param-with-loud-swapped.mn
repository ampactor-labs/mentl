// CROWN — sound-join-sealed-param-with-loud with the branches swapped. Must
// accept.
//
// Falsely refused through the pinned boot (measured 2026-09-23).
effect E { op() -> Int }
fn loud() = op()
fn quiet() = 3
fn g(h: () -> Int with !E, c) = {
  let k = if c { loud } else { h }
  k()
}
handler eh { op() => resume(7) }
fn main() = (g(quiet, true)) ~> eh
