// CROWN — a hole-product is a suspension, and a suspension is a record. Expect
// E_EffectMismatch under the declared !Alloc.
//
// Checked clean through the pinned boot (measured 2026-09-23).
fn add3(a, b, c) = a + b + c
fn mk(x) with !Alloc = add3(1, ??, x)
fn main() = 0
