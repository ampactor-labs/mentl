# Mentl — the program, sequenced by dependency (2026-09-25)

> Written 2026-09-25 on branch `claude/mentl-design-audit-m1mptf`, after the
> audit (`docs/PROCESS-AUDIT-2026-09-25.md`) and the lens
> (`docs/LENS-2026-09-25.md`). Every claim marked **measured** was read off
> the artifact this session; every claim marked **read** was read in the
> source; everything else is design. All results are in: two scratch
> builds, three exploration reports, the space gate, the thread timings.

## Context

The vision (Morgan, 2026-09-25): the medium is the only proposer worth
considering besides a human; multi-cursor, multi-shot exploration of
multiverses; the compiler does the codegen, teaches, empowers; "the gradient".
The lens found the tree is a strong compiler with a thin medium on top (about
a tenth of the source serves the proposing surface, zero proposal fixtures),
and the crown — negation, the one property no peer has — leaks in two ways,
one of which needs no negation at all. The seven things I said bothered me
were: I designed where I should have built; the fan/trail mechanism was taken
on faith; the gradient was never measured; `mentl space` was never opened;
the theorem was a plan, not a proof; the e-graph answer was reductive; one
citation came from an abstract. This plan is built from addressing each of
them, read-only, before a line lands. Nothing under the repo was modified;
experiments ran on copies of the tree under the scratchpad.

## What was learned addressing the seven

### 1. The nested-frame fix, built twice on scratch copies (measured)

**Variant A — pass-through** (tee body, both fanout thunks and the lambda
exit finalize their frame row with only the least-solution cut; no
completion prune; a new `inf_exit_nested` op). Boot compiled the patched
wheel (16.4 s, 1.00 GB); the patched wheel compiled itself to a byte-identical
fixpoint (m3 == m4, 13.7 s / 999 MB against the last march's 15.6 s /
1.00 GB); the blob compile's diagnostic set was unchanged (108 = 108). On the
36 banked probes:

| shape | boot | variant A |
|---|---|---|
| `fn run(f) = { let g = () => f(); g() }`, no handler | traps 134 | **refuses at the root, exit 1**; `run`'s row projects `r` |
| `fn run(f) = (() => f())()`, no handler | traps 134 | **refuses, exit 1**; row `r` |
| `fn par(f, g) = (f()) >< (g())` | row `Pure` | row `r + r` |
| `fn run(f) = (f()) ~> h`, `run(() => fop())`, no `F` handler | traps 134 | **still traps 134**; row still `Pure` |
| `fn run(f) with !E + !F = (f()) ~> h` | traps 134 | still 134 |
| every other row | — | identical to boot |

So pass-through fixes the lambda and fanout shapes and not the tee shape.
The tee's drop is one line later: `read_bound_row` (`infer.mn:9138`, one
caller, the tee at `:4350`) chases the finalized body row; a single-edge open
row is followed as an ALIAS to the free cell, and the reader's `_ =>
mk_ef_pure()` catch-all returns `Pure` — the `_ => <fabricated value>`
surrender fallback CLAUDE.md names. (Hypothesis from the read; variant B
tests it.)

**Variant A is refuted by the wheel** (`mentl check src/main.mn` under the
patched compiler): **163 `E_EffectMismatch`** across 18 modules (lists 18,
pipeline 15, lower 13, query 13, json 11, lsp 11, main 10, driver 8, …), e.g.
`spec_pairs_add(pairs, root, t) with Memory + Alloc` — a function with no
lambda, tee or fanout — projecting `Memory + Alloc + GraphRead`, and
`lib/lists.mn`'s most basic functions carrying `GraphRead`, a compiler
effect no library code performs. The mechanism, from the prune's own
comment and the code: a nested frame that no longer prunes keeps SCRATCH
edges (an instantiated callee's unbound row copy, e.g. `iterate`'s inside
`find`'s tee) behind the mask cell `diff_row` mints; the declaration's exit
prune keeps a bound edge whole ("reads fold it") and never recurses, so the
scratch cell is neither dropped nor quantified, and the first caller whose
context binds it writes that caller's effects into the library function's
published row for every later caller. That is the disconnected-vars class
the second-pass deletion named as a hazard, reached from the other side.
**The measurement is the design correction: nested frames must drop
scratch exactly as declarations do; what they must not drop is a cell the
ENCLOSING signature reaches.**

**Variant B — inherited keep-set** (the adversarial pass's C1): each frame
records its parameter cells (`inf_frame_params`); a nested exit prunes with
`signature_free_roots` over the WHOLE frame stack, so the enclosing
declaration's parameter row cells survive and scratch still drops; plus
`read_bound_row`'s catch-all becomes "the edge to that cell"
(`mk_ef_open([], handle)`). **Results (measured):** the boot compiled the
patched wheel (12.2 s, 1.00 GB, zero new blob diagnostics — 108 = 108).
Every prune shape is fixed, and nothing else moved:

| shape | boot | variant B |
|---|---|---|
| `fn run(f) = (f()) ~> h`, `run(() => fop())`, no `F` handler | traps 134 | **refuses at the root, exit 1**; `run : … with !E + r` |
| `fn run(f) with !E + !F = (f()) ~> h`, `main() with !F = run(() => fop())` | accepts, traps 134 | **`E_EffectMismatch` at check** — `F` under `!F` caught |
| the same, no handler in `main` | traps 134 | refuses, exit 1 |
| `fn run(f) = { let g = () => f(); g() }`, no handler | traps 134 | refuses, exit 1; row `r` |
| `fn run(f) = (() => f())()`, no handler | traps 134 | refuses, exit 1; row `r` |
| `fn par(f, g) = (f()) >< (g())` | row `Pure` | row `r + r` |
| the other 30 rows (vacuous class, controls, annotated forms) | — | identical to boot |

The wheel under the patched compiler: **one** `E_EffectMismatch`
(`synth_proposer.mn:927`, `verify_each_enriched`: declared row lacks
`Cast(GNode)`, which a callback inside its nested frame performs — a real
under-declaration the prune had hidden), against 163 under variant A. The
m3 leg refused on that one site (the blob compile treats it as an error),
so the landing carries the one-word row fix and re-measures the fixpoint
after it. The first patch of variant B had a type error (a frame literal
without the new field at two sites, `E_TypeMismatch` ×2, silently
miscompiled into a trap on every program with the class unarmed) — a
measured reminder that `E_TypeMismatch` in the wheel's own source must
refuse the executable.

Two more measured facts: the blob compile's `T_OverDeclared` set is
identical under variant A (so the wheel's *declared-row* narrations did not
move; the mismatches are all inferred rows that GREW); and the wheel's
declared positive rows are wrong wherever a callback's row was dropped —
which makes SYNTAX's own rule (author negations; infer and project the
positive row) a dependency of the crown arc rather than a style note.

### 2. The fan, the trail, and the first divergence (read, with measurements on existing fixtures)

- **Allowed row at a hole is the enclosing function's AUTHORED clause**
  (`cursor.mn:646-655`, `enclosing_fn_scan` at `:678` → `build_declared_row`,
  `effects.mn:190-234`); an undeclared function yields `Pure`
  (`effects.mn:207-210`); a `~> h` installed around the hole is never read.
  Consumed at `synth_proposer.mn:252`. Measured consequence: in
  `fn main() -> Int = (??) ~> h` an effectful candidate is refused by a
  row nobody wrote.
- **Enumeration** (`synth_proposer.mn:129-160`, `:192-220`): vocabulary
  first — only ZERO-argument `FnScheme`s (`:244-273`); then by target head:
  constructors, one lambda skeleton for `TFun`, `0, 1, -1` for `Int`,
  `0.0, 1.0` for `Float`, domain seeds for refinements (`verify.mn:277`
  `ty_domain`), `[]`/`[??]` for lists, a tuple of holes. A pipe-stage hole
  binds to a `TFun`, so `5 |> ??` with `double`/`inc` in scope yields only
  the lambda skeleton — measured on the nearest fixture as "no proposal".
- **Judgment** (`:756-771`, `:885-895`, verified in the source this
  session): `graph_push_checkpoint()` → `world_top()` → `infer_expr(node)`
  under `infer_ctx ~> fresh_for_inference ~> resume_summaries_ctx` →
  `unify(hole_h, h)` → `verify_after_apply && refinement_admits` →
  `graph_rollback(cp)` → `world_restore(w)` → `branch_join` for the proven.
  Sequential `map` (`:742`); zero `><`, `spawn_task` or `Schedule` in the
  proposer. No `heap_mark` in the bracket, although three comments and a
  ledger entry say there is one. The `verify_candidate` op (`mentl.mn:125`)
  has no caller.
- **The trail records handle + prior value per write** (`Mutation =
  MSetNode(h, old) | MSetRow | MSetBoundary | MFreshNode`, `types.mn:971-979`;
  `graph.mn:541-546`). A candidate's segment is readable only between the
  judgment and the rollback; at rollback `trail_len` returns to the
  checkpoint and the next candidate's first append overwrites entry 0 of
  the previous one. No `GraphRead` op exposes the trail. So the
  first-divergence question is computable in principle (the data is there
  for one candidate at a time) and needs one new op: copy `trail[cp ..
  trail_len)` before rollback, filtering path-compression writes
  (`graph.mn:641-644`). RESIDUE already records that on the existing
  fixtures the hole cell is bound to the return cell before proposing, so
  the unify writes nothing and the diff is empty — the fixture must leave
  the cell genuinely free.
- **Acceptance is a text patch**: `apply_suggestion`
  (`cursor_transport.mn:266-285`) → `replace_span` → `fs_write_file` →
  `driver_check` (full re-derivation). `render_candidate_source` renders
  only `name()`, a bare `VarRef` or a literal; lambdas, lists and records
  render as `??`, so accepting them is a no-op. The IDE regex-parses the
  `Propose:` text (`ide/index.html:991-1033`). No `GraphWrite` op is
  performed on accept; the one site holding both handles is
  `cursor_transport.mn:275-276`.
- **Ranking**: `rank_of` (`:386-403`) = `0.5 + scope_distance_decay(hole,
  decl) + Σ decay over refs`; every non-call shape ranks a flat `0.5`; ties
  keep enumeration order.
- **Verdict**: `VNone | VFill | VAsk(divergence)`; `divergence_of`
  (`:1046-1065`): row split first (`DivRow`), then `candidate_denotation`
  by `node_const` (a zero-arg call's body constant), `DivShape` when
  unreadable, `DivName` when all equal, else `DivValue(ty_domain)`. Measured
  renders (existing fixtures): one survivor fills (`Propose: 1 — inferred
  from the type's integer inhabitants`); a row tie asks "these differ in
  what they may DO — Pure against Log"; same-denotation ties ask "which name
  carries the intent"; value ties ask "nothing here bounds the value — a
  refinement would".
- Stale citations: `src/oracle.mn` does not exist but is cited at
  `graph.mn:291` and `mentl.mn:343`.

### 3. The gradient, measured (read, with measurements on existing fixtures)

- **The only numeric gradient** is the `Float` in `Cursor(Handle, Reason,
  Float)` (`types.mn:1139`), computed by `score_one_position`
  (`cursor.mn:299-316`) as `gates × proximity`: `gates` is the number of
  labels on the ONE suggestion `teach_gradient` returns — 1 in 165 of 165
  measured suggestions — and `proximity` is five literal constants
  (1.0/0.85/0.7/0.4/0.2, `cursor.mn:359-367`). So the "argmax" is a tie
  broken by handle order, in two opposite directions (`cursor.mn:439` strict
  `>` first-wins; `main.mn:1095` reverse-sorted last-wins). Measured:
  `mentl edit tests/frontier/mn-where-badges.mn` focuses `decode_stride` in
  `lib/lists.mn:41` — a function outside the file, the exact failure
  `main.mn:223-231` claims the entry-start caret fixed. `gates_unlocked`
  does not exist as an identifier. The score is printed nowhere.
- **The `Teach:` facet is a constant string** (`main.mn:1545-1548` matches
  `Some(_)` and discards the suggestion), fired wherever the chased node is
  `NBound(TFun)` — including call sites (`fold(`) and a fn-as-value
  (`scale,`), where no annotation can be written.
- **`mentl teach` proposes exactly one kind**: `!E` severance, 165 of 165
  (`src/effects.mn`: 43 `!GraphRead`, 31 `!Alloc`, 10 `!GraphWrite`, …;
  `lib/prelude.mn`: 54 `!SharedMemory`, 18 `!Alloc`), chosen by a literal
  ladder `[!Alloc, !IO, !Network, Pure]` then "first proven"
  (`mentl.mn:572-579`). The name comes from link-wide prevalence
  (`performed_names_of_link`), so `map` is told to add `!SharedMemory`. It
  ignores the authored row (`gradient_candidates`, `mentl.mn:264-267`): `fn
  step(x) with !Alloc` and `fn add_pure(a, b) with Pure` are both told to
  add `!Alloc`. It never reads the one MachineApplicable proposal the
  judgment already banks (`T_OverDeclared` / `tightenables()`, read by
  `audit` and `tighten` but not by `teach`). `with Pure` wins 0 of 165
  (outranked); own/ref are enumerated, "proven" unconditionally, and never
  reached; `ARefined`, `AWrapHandler`, `SuggestReturnType` have no
  constructor site; no repr candidate exists; `T_Gradient` has no emitter
  (catalog string only, listed as MachineApplicable in SYNTAX). "Unlock a
  capability" = a label; for every effect but Alloc/IO/Network it is
  `CProvenAbsence(name)` — the annotation restated; nothing in lower or the
  backends consumes a `Capability`.
- **`mentl where`**: renders `@ f64 (inferred)` / `(pinned)`, `~> h absorbs
  E at <span>`, `tick : Tick op — resume Int ->1 answer`, `>< [Thread] at`;
  the promised `×N` is not rendered (`schedule_word` yields a word only);
  a `let` local answers "not found"; a fn with no tee prints an empty `→`.
- **Facets**: Lede, Query, Effects, Ownership (first param only), Verify
  (count), Why are real projections; Propose is real at authored holes;
  Topology is computed (`pipe_context_of_handle`) and ignored by
  `render_at`; Teach is a computed predicate with constant text. The
  voice's `gradient_next` is always `None` (`voice.mn:825`), so
  `render_teach_arm`/`render_unlock_arm` render empty bodies (RESIDUE:236
  already open); `teach_here`/`teach_error`/`teach_unlock` ops have no
  performer.

### 4. `mentl space` opened headless; threads timed (measured)

- **The IDE gate is GREEN on both legs in this container** — the first
  browser run since 2026-07-29: leg 1 (node twin) 8.7 s, all six faces
  pass; leg 2 (headless Chromium over `mentl space`) `SMOKE exit=0
  tasks=259 watlines=4328 ms=2049`; whole gate 157 s. Leg 2 skipped on
  every prior board only because the script tests for the literal command
  name `google-chrome` (`tools/ide-gate.sh:20`); a two-line wrapper on PATH
  un-skips it. The page loads (`ide/index.html` 67,569 bytes,
  `mentl-ide.wasm` 1,787,491 bytes over `mentl space` on 7378); the Aspect
  Ring is empty until a caret is placed. The screenshot shows the editor's
  code with its spaces collapsed (`fnmain()withMemory+Alloc=`) — a headless
  font artifact or a CSS defect; unverified, to check in a real browser.
- **What the page really does** (read, `ide/wheel-worker.js`):
  `session-call` re-instantiates the wasm and zero-fills memory on every
  call (`:278-288`) with `tasks:0` hard-coded (`:296`); "delta update" is a
  whole-file replace in a JS map; accept is a client-side text splice; the
  page's pinned `ide/mentl-ide.wasm` is the 2026-07-29 wheel (1.79 MB) while
  the boot is 2026-09-21 (2.57 MB). So PLAN §11.2's "session alive across
  actions" holds for the Worker, not the graph; "sub-50 ms address
  projections" has no measurement anywhere (each projection is a full
  `_start`; the only recorded browser number is a 0.9–2.0 s compile); and
  the caret renders at most seven facets (no verb/topology line; the row
  suppressed when Pure).
- **Threads are spawned and do not run in parallel** (measured): two
  branches of 1.5e9 tail-recursive iterations, `(burn) >< (burn)` bare vs
  `~> parallel_compose`, on 4 cores — bare: wall 2.34 s / user 2.34 s;
  threaded: wall 2.26–2.33 s / user 2.30–2.40 s. Two `wasi_thread_spawn`
  references are emitted and the runner spawns real OS threads
  (`tools/runner/src/main.rs:170-227`, one Store + instance per thread over
  the shared memory), and the thread gate's strace control sees the extra
  clones ("parallel reads 6 vs sequential 4"). Wall ≈ user means the two
  branches did not overlap. Root cause unmeasured (candidates: spawn-then-
  join per branch instead of spawn-all-then-join-all in `run_fanout_thunks`,
  `lower.mn:2968-3003`; a wait in the spawned thread's start path). The
  thread gate itself is RED here ("ratchet: delta 1 exceeds 0 — per-branch
  spawning is back (1 decl 10, 61 decls 11)"): its ratchet counts OS clones,
  which move with the host.
- `><` inside a callee is `Seq` regardless of the caller's install (the
  frame fence, `lower.mn:1882-1885`, SYNTAX §`><`), so a reusable HOF can
  never schedule — the open peer `Hβ.lower.schedule-specialized-callee`.

### 5. The theorem: lemma (i) through a mask, done by hand

Done, and moved to its one home: LENS §2.3 carries the hand proof (the bind through a mask splits into a ground check on the folded value and a push `G_v := G_v ∩ (G ∪ mask)`; masks on a path accumulate by union; two paths meet by intersection; no false-mismatch class because a gate refuses only a contradicting write), the twelve shapes searched without a fifth counterexample, and the base calculus verified from the paper bodies (Bauer–Pretnar and Hillerström–Lindley are both monomorphic set-row calculi with handler subtraction; no published proof combines `lacks`-style gates with it, so lemma (i) is new). The three-walks-agree obligation it names was measured the same day by the nested-frame fix's refuted first form (scratch behind a mask cell, neither dropped nor quantified).

### 6. The e-graph: the sharper question, answered

The six rules (`egraph.mn`): add/mul/sub/div identity, mul-absorb, integer
const-fold — arithmetic peepholes on the lowered tree; `saturate_range` is
dead on the fan path. SYNTAX refuses redundant forms at the surface and
`fmt` has a fixpoint, so SPELLING variance never reaches the graph; the only
form variance that exists is SEMANTIC (two bodies, one denotation:
`fold(0, add, xs)` and `sum(xs)`). The proposer already classifies that tie
(`DivName`) and §11.1 already says a same-denotation tie is a meaning tie at
the intent altitude that the medium ASKS about. So the e-graph's role in the
vision is one thing: to DECIDE same-denotation cheaply, by canonical root —
the canon column the medium already has, applied at the proposer (dedupe
survivors by `graph_canon_at` root before classifying) — plus a rule set
that grows with the workload (stage fusion, fold laws) and `~> Cost` off the
default chain. It is not a search structure and not a separate subsystem.
The miscompile (absorb drops a trap) is closed by partiality as a row fact.
"Delete it" loses the same-denotation read; "keep it as an optimizer" keeps
a miscompile and a dead search role.

### 7. Provenance

Bauer–Pretnar and Hillerström–Lindley verified from the paper bodies (text
extracted from the PDFs). Biernacki unverified (403). LENS §2.3 is corrected
accordingly in landing A0.

## The program

The program follows, in seven tracks (A crown · B threads · C proposer ·
D gradient · E space · F process · G workload), then the order, the
verification per landing, and the definition of done.

---

# THE PROGRAM

Ordered by dependency, not by size. Each landing names its mechanism, its
files, and the gate that must be seen RED before it lands (§9.11). One
landing = whole arc → verify → board → repin (the cadence law). Every
dispatched agent runs Fable 5.1 at max effort (Morgan, 2026-09-25).

## Track A — the crown: negation sound under polymorphism

**A0 · Doc truth for what this session measured** (docs only; first, so no
one rebuilds a refuted design). LENS §2: base calculus verified from the
paper bodies; variant A refuted by the wheel (163); variant B's result;
the `read_bound_row` catch-all named. LENS §7.1/§7.3: the two scratch
builds as kills. RESIDUE: new peers —
`Hβ.infer.nested-frame-prune-drops-parameter-row` (the class, its two
probes, the 163 exposure), `Hβ.effects.declared-positive-rows-under-count-
callbacks`, `Hβ.threads.perform-inside-spawned-branch-traps`,
`Hβ.ide.session-call-reinstantiates-per-call`, `Hβ.ide.pinned-wasm-lags-
boot`, `Hβ.teach.constant-facet-and-one-kind`, `Hβ.synth.acceptance-is-a-
text-patch`; PLAN §7 lines corrected: `TCont` world = handler frame cell;
thread gate ratchet is host-sensitive; ide-gate leg 2 green 2026-09-25.
Stale citations (`oracle.mn` ×2, the `heap_mark` comments ×3) deleted.

**A1 · Arm `E_EffectMismatch`.** LANDED 2026-09-25 at pin 13df4844, with A2 (see LEDGER). One arm in `diag_refuses`
(`src/types.mn`), licensed by the wheel's zero census (measured: the wheel
compiles with zero `E_EffectMismatch`). Re-run every fixture that mentions
the class (59); re-bank the frontier expectations that assumed narration
then a run (`adv-annot-neg` and friends move from "exit 7" to "exit 1").
Gate seen red: `tests/lens/negation/adv-annot-neg.mn` runs today (exit 7)
after printing the refusal; after A1 it refuses (exit 1, zero WAT).

**A2 · The nested-frame prune keeps the enclosing signature's cells
(rule 0, variant B).** LANDED 2026-09-25 at pin 13df4844. The landing also
closed the feedback spec's false `Alloc` charge (branch A of the banked fork
— arming made it a false refusal of `mn-feedback-iir`), deleted a dead abort
stub the fixed prune exposed at the wheel's root, and re-banked two frontier
expectations the unarmed era had canonized. Mechanism (`src/infer.mn`): the `InferFrame` record
carries its signature cells (`params`), set by one op (`inf_frame_params`)
after `mint_params` in `FnStmt`, `LambdaExpr` and `HandlerDeclStmt`; a
nested exit (`inf_exit_nested`: tee body `:4348`, both fanout thunks
`:4901`/`:5035`, the lambda `:3409`) prunes with
`signature_free_roots(⋃ frames' params)` so a parameter's row cell survives
and judgment scratch still drops; `read_bound_row` (`:9138`) answers the
edge (`mk_ef_open([], handle)`) instead of fabricating `Pure`. About
forty lines; two ops; no representation change. Gates seen red: the seven
prune probes (no-handler forms refuse at the root instead of trapping;
`run`'s row projects the parameter's variable), `adv-tee-direct-kept`
stays refused, m3 == m4, the march's RSS ratchet, and — the control that
variant A failed — `mentl check src/main.mn` with ZERO new mismatches.
Measured result of the scratch build (variant B, §1 above): all five prune
shapes fixed; `adv-mask-partial` refuses at check; every other probe row
identical to boot; one wheel signature exposed (`verify_each_enriched`
lacks `Cast`), which this landing fixes in the same commit before the
march; the fixpoint is re-measured after that one-word change.

**A1b · Arm `E_TypeMismatch`** — LANDED 2026-09-25 at pin 3cc9fdec. (`Hβ.diag.type-mismatch-is-unarmed`): the
wheel's census is zero, and the A2 build measured the harm — two narrated
type errors in the compiler's own source became a compiler that trapped on
every program. Same licence and the same frontier re-derivation as A1.

**A3 · The gate is carried by the cell (LENS §2.2, second form).**
`src/graph.mn`: the gate column (`gate_col : handle -> Option(EffRow)`),
its read, and `graph_bind_row`'s JOIN/occurs arms checking it.
`src/effects.mn`: `unify_row_canonical`'s five arms check + push (the two
`EtAll`-writing arms become gate installs); `gate_meet`; `diff_row`'s
push-through (`G_v := G_v ∩ (G ∪ mask)`); `row_join` checks.
`src/infer.mn`: pre-registration installs the declared gate on the decl's
row cell and its signature's row vars (`build_declared_row` at prereg);
`enforce_row_gate` walks `resolve_row`'s free terminals through masks and
never consults `merge_chased_row`'s synthetic node; the D2 absent write
(`:2803`, measured inert) deleted; the argument edge (`:4064`) checks the
folded row; the three `graph_finalize_row` callers check; `build_inst_
mapping` and the three `mint_row` sites copy the gate; `T_OverDeclared`
narrates only on a ground body row. `src/pipeline.mn`: the root gate reads
the install chain (`Hβ.effects.reachable-perform-with-no-install-compiles`
rides along). Gates seen red: every LENS §2.6 fixture, the crown battery
(62 stays green), the `try_with_abort_catch` refusal (expected; its
positive row is deleted in A4), the wheel's four `declared-row-hof` sites,
the generalize-twice mask fixture (`fn run(f) with !E + !F = (f()) ~> h`
at two call sites). Order inside: column → prereg install → writers →
copies, one landing, because a gate that checks at some writers and not
others is the vacuous class moved.

**A4 · The positive row is inferred and projected; only negations,
instance pins and genuine narrowings are authored**
(`Hβ.syntax.positive-row-is-authored-by-hand`; SYNTAX §«A signature is not
an inventory»). Measured need: variant A exposed 163 wheel declarations
whose positive rows omit what their callbacks perform; under A2/A3 the
medium refuses itself until they are fixed, and widening 163 hand-copied
rows is the disease. Mechanism: `mentl tighten` already authors row
patches — extend it to DELETE positive names the body proves (keeping `!E`,
`E(k)`, and any name the author marks as a deliberate narrowing), run it
across `src/` and `lib/`, and let `mentl where`/the address surface project
the inferred positive row. `CsWideRow` ratchets to zero. Gate: the blob
compile's `T_OverDeclared` count (108 today) falls to zero; the march holds.
DECIDED (Morgan, 2026-09-25): infer and project the positive row; only
negations, instance pins and deliberate narrowings stay authored.

**A5 · The world on the continuation gets its static rule** (LENS §2.4):
`inf_current_world()` answers the union of frame row cells from the perform
site to the nearest lexically enclosing absorbing tee (read from the frame
stack, not the handler's own frame), and `ResumeExpr` charges `world(k) −
handled(h)` into the arm. Gates: `sound-masking-negation` and
`adv-arm-resume-world` stay green; a new fixture where the remainder
performs `F` (not handled by `h`) and the arm's row must carry `F`. The
cross-image refusal (`E_ResumeWorldMismatch` at load) waits on a
first-class continuation value (band B) and is written nowhere as landed.

**A6 · Install identity — the modal world-index** (LENS §2.5), sequenced
after A3: the install edge's handle is the instance; `diff_row`/`eff_forbids`
instance-precise over installs; the perform site's evidence carries the
install handle; distinctness by INSTALL. Crown crucibles per rule,
red-first. Not before A3.

**A7 · The theorem's mechanical half becomes fixtures**: the writer-set
completeness table as a census shape (every `graph_bind_row`/`bind_edges_
to`/`graph_finalize_row` caller checks — a grep today, a `mentl query`
facet at landing), the three-walks-agree fixture (quantifier vs
`flatten_row_edges` through a mask cell), and LENS §2.3's lemma (i)
written into the doc as proven-by-hand with the twelve searched shapes.

## Track B — SPACE: threads

**B1 · A perform inside a spawned branch traps** (measured: `current_id()`
under `~> parallel_compose` → memory fault at 0x100000000 in
`ev_declaring_node ← work ← compose_0 ← wasi_thread_start`; the
identity fixture passes only because `thread_id()` is a direct WASI op).
The spawned instance's per-instance globals (the world chain top, the
evidence base) start at zero. Mechanism: the task record the spawn hands
over carries the spawning frame's world-chain top (the chain lives in the
shared image, so only the head handle must travel); `$wasi_thread_start`
installs it before calling the thunk. Gate seen red: a threaded branch
performing an op absorbed by a handler installed outside the fanout
returns the handler's value (traps today). Design consequence recorded:
`><` under `~> Thread` is unusable for effectful branches until this lands,
which the crown's `!Thread` crucibles never exercised.

**B2 · The thread gate measures the wheel, not the host.** The ratchet
`judge_spawn_delta_max: 0` counts `clone` under strace and is RED in this
container (delta 1: 1 decl → 10, 61 decls → 11) with no wheel change.
Replace with the medium's own count: emitted `spawn` sites and the
judgment's spawn count as a `mentl query` facet; keep the strace leg as a
control, never as the ratchet.

**B3 · Parallelism is real and measured 2×** (`work()` branches: bare 4.50 s
wall / 4.47 s user; `~> parallel_compose` 2.31 s / 4.57 s). One anomaly
stays open: branches that are direct calls with literal arguments measured
no speedup (2.34 vs 2.26 s wall at equal user time) — one probe, root
unmeasured; bank it as a fixture that times both shapes.

**B4 · `Hβ.lower.schedule-specialized-callee`**: the frame fence makes a
`><` inside a callee `Seq` regardless of the caller's install, so no
reusable HOF and no fan can schedule. Needed by C9; sequenced after B1.

## Track C — the proposer becomes the medium proposing

**C1 · The hole's allowed row is the inferred row at the hole, including
the tees installed around it** — LANDED 2026-09-25 at pin 3cc9fdec (`cursor.mn:646-655`; today the authored
clause, `Pure` for an undeclared fn, and `~> h` never read). One-line
class; measured today: an effectful candidate refused by a row nobody
wrote.

**C2 · A tie past a small width renders its divergence and the count, not
the members** (`main.mn:1507-1519`; §11.1's "never a list").

**C3 · Enumerators grow in the vision's order**: (i) function REFERENCES for
a `TFun`-typed hole (rule 1 of SYNTAX §«Function literals»: reference before
mint) — LANDED 2026-09-25 (see LEDGER): `5 |> ??` yielded only a lambda
skeleton because one-argument fns were skipped; references are enumerated
now, the skeleton is offered only when no reference is proven, a
function-typed tie asks the behavior question, and the vocabulary is
searched outward from the hole's module (own, then authored imports, then
the rest of the link), stopping at the first ring that proves a survivor.
Building it measured two more defects, both fixed in the landing: the
hole's target was read unresolved, so every one-parameter fn passed the
head index, and `r.own` parsed to a field with an empty name. (ii)
vocabulary calls with argument holes (`f(??)`), so a configured stage can
be proposed; (iii) bounded sub-hole recursion (two moves); each measured by
a proposal fixture first.

**C4 · Acceptance draws an edge.** `apply_suggestion`
(`cursor_transport.mn:275-276`) is the one site holding the hole handle and
the survivor handle; it performs a `GraphWrite` (a `Proposal` edge carrying
the survivor and the Reason) before the text splice, and the persisted
image carries it across the re-derivation. `render_candidate_source`
renders lambdas/lists/records (today `??`, so accepting them is a no-op).
Gate: accept a survivor, re-derive, `mentl why` at the position walks to
the accepted proposal.

**C5 · Same-denotation by canonical root; partiality as a row fact.**
Survivors dedupe by `graph_canon_at` root before `divergence_of` (the
e-graph's one proposal role); `/` and `%` carry `Trap`, discharged by a
`NonZero` refinement, so `rw_mul_absorb` fires only when the dropped
operand's row subsumes pure — closing the measured miscompile
`(1 / 0) * 0 → 0`. The rule set grows only from the workload (stage fusion
first); `~> Cost` off the default chain; `extract_chase`'s silent
truncation refuses.

**C6 · The first divergence from the trail.** A `GraphRead` op copies
`trail[cp .. trail_len)` before `graph_rollback` (`synth_proposer.mn:761`),
filtering path-compression writes; `divergence_of` reads the earliest cell
two survivors bound differently and renders it in the developer's
vocabulary. Fixture: a hole whose cell is genuinely free at propose time
(RESIDUE records that the existing fixtures bind it to the return cell).

**C7 · Proposal fixtures join the frontier harness** (zero exist): hole,
expected verdict (fill / ask with a named divergence / none), expected
question; one per enumerator, one per divergence arm, one per landing
above.

**C8 · Comment truth in the proposer**: the `heap_mark` claims (three
comments, one ledger line), `verify_candidate` (no caller), `oracle.mn`
(two citations to a file that does not exist) — the comment-ref gate
extended to module citations.

**C9 · The fan is written `(c) >< (c) ~> Schedule`** once B4 lands: width a
handler decision, the segment walk sequential by property, not constant.

## Track D — the gradient and teach

**D1 · Teach reads what the judgment already proved.** LANDED 2026-09-25 (with D2; see LEDGER). `gradient_
candidates` (`mentl.mn:264-267`) ignores the fn's own row and the banked
`T_OverDeclared` (`tightenables()`, read by audit and `tighten` but not by
`teach`), so a fn declared `with !Alloc` is told to add `!Alloc` (measured:
`step`, `add_pure`, `silent`). Fix: candidates exclude what is authored;
the tightening the judgment banked is the first suggestion; the ladder
(`[!Alloc, !IO, !Network, Pure]`, `mentl.mn:572-579`) becomes the argmax
over distinct capabilities unlocked.

**D2 · The `Teach:` facet says what it found** — LANDED 2026-09-25 (`main.mn:1545-1548`
discards the suggestion and prints a constant) and fires only where an
annotation can be written (decl sites; today at any `TFun`-typed node,
including `fold(` and a fn-as-value).

**D3 · The score is a gradient** (`score_one_position`, `cursor.mn:299-316`:
`gates × proximity` with `gates` = 1 in 165 of 165 measured suggestions,
proximity five constants, ties by handle order — measured: `mentl edit` on
a fixture focuses `decode_stride` in `lib/lists.mn`, outside the file).
Positions enumerate the file's own decls; `gates` counts distinct
capabilities; the two tie-break orders (`cursor.mn:439` strict `>`,
`main.mn:1095` reverse) become one.

**D4 · The annotation space the docs promise**: refinement (`ARefined`, no
constructor site), repr pin (no candidate), return type
(`SuggestReturnType`, never constructed), own/ref (unreachable behind the
ladder), the `T_Gradient` emitter (catalog string only). Each a candidate
kind proven through the same checkpoint bracket; each with a fixture whose
expected suggestion is that kind.

**D5 · `where` renders what SYNTAX promises**: `>< [Thread ×N]` (the `×N`
is not rendered), `let` locals (today `not found`), no empty `→` lines; the
voice's `gradient_next` is either filled (`voice.mn:825` always `None`) or
deleted with its two dead arms.

## Track E — the felt surface (`mentl space`)

**E1 · The IDE gate runs both legs on every board.** The browser discovery LANDED 2026-09-25 at pin 3cc9fdec; wiring the gate into `state.sh` and the CI board stays with F2. Leg 2 skipped on every
prior board only because `tools/ide-gate.sh:20` tests for the literal
`google-chrome`; it takes a browser path (`MENTL_CHROME`) and finds
Chromium; measured green here (`SMOKE exit=0 tasks=259 ms=2049`, 157 s).
`tools/state.sh` and the CI board run it.

**E2 · The session keeps its graph.** `ide/wheel-worker.js:278-288`
re-instantiates and zero-fills memory per `session-call` with `tasks:0`
hard-coded; the resident session keeps the instance and applies the delta
to the live graph; `ide/test-shim.mjs` gains a timer so "sub-50 ms" is a
measurement (today the only number is a 0.9–2.0 s compile).

**E3 · The page runs the boot.** `ide/mentl-ide.wasm` is the 2026-07-29
wheel (1.79 MB) against a 2026-09-21 boot (2.57 MB); `mentl space` serves
`boot/mentl.wasm` (or the pin is the boot by construction).

**E4 · Eight facets at the caret** (seven today: topology is computed by
`pipe_context_of_handle` and ignored by `render_at`; the row is suppressed
when Pure).

**E5 · The whitespace collapse** in the headless screenshot
(`fnmain()withMemory+Alloc=`) is verified in a real browser and fixed if it
is CSS.

**E6 · Accept through the graph on the page** (C4), the Why walk, then the
Severance Map (Arc G) after A3 — three-colour, never two.

## Track F — process (from the audit; DECIDED: included now, Morgan 2026-09-25)

**F0 · The loop stops re-deriving itself** — LANDED 2026-09-26 (LEDGER; no
repin). Every gate leg memoizes its green under the sha of what it reads,
the text ratchets run before any compiler, and a pin that did not move
writes nothing: a byte-identical repin went from ~20 min to 22 s.

**F1** CLAUDE.md to ≤150 always-loaded lines; DESIGN.md / MILESTONE.md /
decision records hold the rest (drafts: `docs/proposals/CLAUDE.next.md`,
`MILESTONE.next.md`). **F2** The CI board (`docs/proposals/board.yml`,
verified in this container) runs verify, march, crown, proof-exactness,
both IDE legs, the thread gate after B2. **F3** Every law that can be a
gate becomes one, with a ratchet end date. **F4** The fixture classes this
plan adds (proposals, threads, space timing) are board rows.

## Track G — the flagship workload: Pulse (DECIDED, Morgan 2026-09-25)

The felt path is walked first (the FELT-PATH-FIRST law): Pulse's first
scene (`examples/pulse/`, the DSP file from `docs/proposals/MILESTONE.next.md`:
`!Alloc`, `<~`, `><` in one program) authored in `mentl space`, every hole measured by the
proposal fixtures of C7, the `!Alloc`/`!Thread` path proven by the crown,
and the Severance Map demo (thirty seconds: a module green, one call added,
the band turns red with the Reason naming the call). Each C/D/E landing is
accepted against this scene, not against the wheel alone.

## Order

```
A0 ─ A1 ─ A2 ─┬─ A3 ─ A5 ─ A6 ─ A7
              └─ A4 (medium-authored sweep, with A2/A3)
B1, B2, D1, D2, D3, E1, E3   — small, independent, early (any order after A0)
C1, C2                        — one-line classes, early
C3 … C7, C9, D4, D5, E2, E4, E6 — after A3 (proposals on !E paths are worth
                                  less until the crown is sound)
F1–F4                         — when Morgan says
G                             — its first scene is the acceptance test for
                                C/D/E; its felt walk opens each of them
```

## Verification (per landing, all on the board)

- `bash tools/state.sh` whole board; `bash tools/march.sh` (m2 == m3, the
  m4 leg on failure; `--fixpoint` once per landing until determinism is
  probed on the board); `bash tools/crown-gate.sh`; `bash tools/ide-gate.sh`
  both legs; `bash tools/thread-gate.sh` (after B2, the wheel's count).
- `tests/lens/negation/EXPECT.md` re-measured after A1/A2/A3: every
  `134` becomes a `1` with `E_EffectUnhandled` or `E_EffectMismatch`; every
  leak row (`exit 7/8/10/11/14`) becomes a refusal; the controls stay.
- `mentl check src/main.mn`: zero diagnostics after A2 (variant A's 163 is
  the red control), zero after A4's sweep.
- Proposal fixtures (C7): measured verdict and question per hole.
- Thread fixtures (B1, B3): the effectful branch returns; the timing pair.
- Space: both legs green; the shim timer; the boot served.

## Definition of done for this program

Every board gate green including both IDE legs and the wheel-counted
thread gate; crown ≥ 62 + the §2.6 fixtures; the wheel at zero diagnostics
with positive rows inferred; proposal fixtures ≥ 20 all green; the first
scene of the flagship workload authored in `space`, one proposal accepted
through the graph and walked by `why`; LENS §2.3's theorem carried with
its mechanical obligations as fixtures.
